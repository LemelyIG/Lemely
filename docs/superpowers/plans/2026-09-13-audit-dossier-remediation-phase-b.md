# Audit Dossier Remediation — Phase B (`feat/native-structural-capability`) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land every Phase B row of the remediation ledger (42 rows, packets B1–B7): launch/loading skeletons and splashes, the navigation model (history trap, scroll lock, scroll restoration, back control, View Transitions, exits), thumb-reach navigation (bottom tabs, sidebar retrofit, edge-swipe), gestures, feedback/capability APIs, offline upload queue + persisted cache + idempotency + review pagination, and the native-feel e2e spec that proves it.

**Architecture:** Additive changes to the Vite/React PWA (`web/`), three new `web/src/lib/` families (`nav/`, `gestures/`, `offline/`, plus `pdf/`, `scanner/`, single-file `haptics.ts`/`wakeLock.ts`/`share.ts`/`badging.ts`), one backend migration + one header on `POST /student/uploads`, one backend pagination change on `GET /teacher/review`, DESIGN.md §9/§12/§15 edits. New runtime deps: `workbox-background-sync`, `@tanstack/react-query-persist-client`, `@tanstack/query-async-storage-persister`, `idb-keyval`. No `opencv.js` (Task 8 explains). Each packet is one or more commits; the ledger row status is updated in the same commit.

**Tech Stack:** React 19 + react-router-dom 7.18 (data router, `viewTransition`, `ScrollRestoration`), Tailwind 4.3, vite-plugin-pwa (`injectManifest`, workbox 7.4), Vitest (Node environment, no jsdom), Playwright 1.62 (device emulation), FastAPI + SQLAlchemy + Alembic (`lemely/db/migrations/versions`, head `0035_merge_parent_invites_review`).

**Spec:** `docs/superpowers/specs/2026-09-10-audit-dossier-remediation-design.md` (Phase B section) · **Ledger:** `docs/superpowers/specs/2026-09-10-audit-dossier-remediation-ledger.md` (42 rows with `phase = B`) · **Phase A plan (mirror):** `docs/superpowers/plans/2026-09-10-audit-dossier-remediation-phase-a.md`

## Global Constraints

- Branch `feat/native-structural-capability` (exists, branched from `develop` at `c70dd38d`), PR into `develop`, never `main`. Worktree `.claude/worktrees/feat-native-structural-capability`; all file and git operations happen there.
- Signed commits: `git commit -S`; conventional scopes (`feat(web):`, `fix(pwa):`, `feat(api):`, `feat(db):`, `docs(design):`, `test(e2e):`, `ci(web):`); `pre-commit run --all-files` (inside `source .venv/bin/activate`) before every commit; trailer `Claude-Session: https://claude.ai/code/session_01CxbrmXri2Fm1p2KFgjL3rr`.
- **Never run the full test suite locally** (memory `no-full-test-suite-locally`). Web: `npm run typecheck && npm run lint && npx vitest run <named files> && npm run build`. Backend: `pytest --no-cov tests/<named file>` + `ruff check`. CI runs everything.
- **Test-architecture constraint, hard rule (D3.20):** `web/vitest.config.ts` is `environment: "node"`, `include: ["tests/unit/**/*.test.ts"]`. No jsdom, no `@testing-library`, no `.test.tsx` anywhere. Every new unit test is a plain `.ts` file using either (a) source-text assertions (`readFileSync` + `stripComments` from `tests/unit/support/jsxSource.ts`, the pattern in `tests/unit/outletBoundary.test.ts` and `tests/unit/loadingTiers.test.ts`) or (b) pure-function extraction (the pattern in `tests/unit/onboardingGate.test.ts`: the decision lives in an exported function, the component only wires it). Component *behaviour* coverage lives in Playwright only — `web/e2e/native-feel.spec.ts` (Task 12). Phase A's plan predates this constraint and names one `.test.tsx` in its Task 2; do not copy that.
- Implementers are `executor` model=sonnet; reviewers `code-reviewer` model=opus; verifier `verifier` model=opus. Main orchestrating session (sonnet, ralph + `Skill(orchestrator)`) never writes code (memory `delegate-by-model`).
- Every implementer brief starts with: `Skill(superpowers:test-driven-development)`, `Skill(superpowers:verification-before-completion)`, `Skill(vitest-testing-patterns)`, `Skill(react-best-practices)` (any `.tsx`), `Skill(checklist-discipline)` + the task's domain skills (table below). Invoke before writing code; follow; do not summarise it back.
- DESIGN.md §14 rule 3: tokens only, no arbitrary Tailwind values. Where this plan needs a new dimension (sidebar width/breakpoint, bottom-nav height) it adds a token or `@utility` in `index.css` first, exactly as A1 did for `--fs-field`/`text-field`.
- **Line numbers below are approximate; re-grep at the start of every task.** They were read on `develop` at `c70dd38d`. Phase A changed `nav-shells.tsx`, `index.css`, `routes.tsx`, `index.html`, `sw.ts` and every portal `index.tsx`; the numbers here are from *after* those changes but will drift again as Phase B lands.
- Ledger update in every commit: set the row's `status` to `done B<n>` and `evidence` to the verification command that passed. The phase-close verifier (Task 13) replaces `done B<n>` with `done <short sha>`.
- Copy rules: no em-dashes in user-facing strings (`npm run check:copy` must stay at 0 findings); DESIGN.md error-copy voice (specific, no "Something went wrong").
- CI guards from Phase A must stay green: `web/scripts/check-native-invariants.mjs` (18 checks; runs in `npm run lint`) and `web/scripts/check-bundle-budget.mjs` (200KB gzip per chunk, `postbuild`). Its two `--phase-b` checks (zero `fallback={<RouteFallback` in `routes.tsx`; zero `document.body.style.overflow = "hidden"`) become unconditional in Task 2. Task 11 lowers the budget to 150KB; the current largest chunk is `CorrectPaper-*.js` at 185KB gzipped (`index-*.js` 133KB, `chart-data-table-*.js` 73KB), so Task 11 must actually shrink it.
- BottomNav tab labels (user decision): student `Overview · Correct · Classes · Profile · More`; teacher `Overview · Grading · Review · Classes · More`.
- Backend service is single-instance FastAPI (`anyio.to_thread` for blocking work, no distributed lock anywhere in `lemely/`): idempotency in Task 9 uses a DB unique index, not a lock.
- If a packet needs a capability no listed skill covers, invoke `Skill(skill-coach)` to create it before implementing (orchestrator's gap rule, design spec "Orchestration protocol" item 1).

## Skills per packet

Process, every packet (in Global Constraints above, not repeated per task): `superpowers:test-driven-development`, `superpowers:verification-before-completion`, `vitest-testing-patterns`, `react-best-practices` (any `.tsx`), `checklist-discipline`.

| Packet | Domain skills |
|---|---|
| B1 (pre-mount shell re-verify, skeletons, splashes) — Tasks 0, 1 | `pwa-expert`, `react-performance-optimizer` |
| B2 (history trap, scroll lock, ScrollRestoration, back control, View Transitions, exits) — Tasks 2, 3 | `react-view-transitions` (project-vendored at `.claude/skills/react-view-transitions/SKILL.md`, present at `c70dd38d`; verify before invoking), `mobile-ux-optimizer`, `react-best-practices` |
| B3 (bottom CTA bar, BottomNav, SidebarNav retrofit, edge-swipe) — Task 4 | `native-app-designer`, `mobile-ux-optimizer`, `ux-heuristics` |
| B4 (gestures, long-press) — Tasks 5, 6 | `mobile-ux-optimizer`, `native-app-designer`, `error-handling-patterns` |
| B5 (optimistic grading, haptics, Wake Lock, Web Share, Badging, orientation) — Task 7 | `error-handling-patterns`, `pwa-expert` |
| B5 (scanner quality, multi-file picker) — Task 8 | `computer-vision-pipeline`, `performance-profiling`, `error-handling-patterns` |
| B6 (idempotency, review pagination backend) — Task 9 | `rest-api-design`, `postgresql-optimization`, `error-handling-patterns` |
| B6 (offline queue, Background Sync, persisted cache) — Task 10 | `pwa-expert`, `caching-strategies`, `error-handling-patterns` |
| B6 (prefetch, pdf-lib split, budget 150KB, review load-more) — Task 11 | `performance-profiling`, `react-performance-optimizer`, `caching-strategies` |
| B7 (e2e) — Task 12 | `playwright-e2e-tester`, `webapp-testing` |
| DESIGN.md edits (Tasks 1, 3, 4, 6, 7) | `technical-writer`, `writing-guidelines` |
| Reviewers (opus, every packet) | `code-review-checklist`; Task 9/10 additionally `security-auditor` |
| Phase close (Task 13) | `ai-slop-cleaner`, `merge-readiness` |

## Orchestration

Main session: `/model sonnet`, then `/oh-my-claudecode:ralph` against this plan file, invoking `Skill(orchestrator)` as coordinator. The orchestrator decomposes into the packets below, dispatches `executor` (sonnet) / `code-reviewer` (opus) / `verifier` (opus) per packet (protocol steps a–e in the design spec), and synthesises results into the ledger. Ralph exits Phase B only when every ledger row with `phase = B` is non-`pending` and verifier evidence is recorded.

Parallelism (up to 3 packets when file ownership is disjoint):
- **Wave 1 (parallel):** Task 1 (B1), Task 7 (B5 feedback/capability), Task 9 (B6 backend). Disjoint: B1 owns `routes.tsx`/`loading-shapes.tsx`/`state-views.tsx`/`generate_icons.mjs`/`index.html`; B5 owns `FlashcardReview.tsx`/`confirm-modal.tsx`/`CameraCapture.tsx`/`PaperResult.tsx`/`useNotificationApi.ts`/`sw.ts` push handler; B6 backend owns `lemely/`.
- **Wave 2 (sequential):** Task 2 → Task 3 → Task 4 → Task 5 → Task 6. B2 and B3 both touch `nav-drawer.tsx`, portal `index.tsx` files and `index.css`; B4 touches `nav-drawer.tsx` (drag-dismiss) after B2's exit/history work and consumes B3's `useDragGesture`. **Never run Tasks 2–6 in parallel with each other.** Task 8 (scanner) may run in parallel with Tasks 2–6 (it owns only `CameraCapture.tsx`, `file-drop.tsx`, `lib/scanner/`, `lib/pdf/`) but must follow Task 7 (same file: `CameraCapture.tsx`).
- **Wave 3:** Task 10 and Task 11 (B6 web) after Tasks 8 and 9; they may run in parallel with each other only if Task 11's `CorrectPaper.tsx` prefetch edit and Task 10's `CorrectPaper.tsx` queue banner are landed by the same executor — otherwise sequential (10 then 11).
- **Wave 4:** Task 12 (B7) last, after every UI packet. Task 13 closes the phase.

Phase end: `superpowers:finishing-a-development-branch` → push + PR into `develop` (body ends with the session URL), `merge-readiness` skill report. User merges; Phase C plan is written against merged `develop`.

---

### Task 0: Branch bootstrap, re-verify, ledger wiring

**Files:**
- Modify: `docs/superpowers/specs/2026-09-10-audit-dossier-remediation-ledger.md` (status column only)

**Skills:** none beyond process skills.

- [ ] **Step 1: Confirm branch state.** Run: `git status --short --branch && git log --oneline -1`. Expected: `## feat/native-structural-capability`, clean tree, HEAD `c70dd38d`. Run `cd web && npm run typecheck && npm run lint && npm run build` once to confirm the baseline is green (the brief says it is; this is the evidence).
- [ ] **Step 2: Re-verify the pre-mount shell rows.** The dossier was written against a worktree 227 commits behind. On `c70dd38d`, `web/index.html:82-593` already carries an inline pre-mount shell (tier 2 portal-chrome skeleton + tier 3 slow-load screen), colours resolved at build time by `web/vite/preMountShell.ts` from the `--paper`/`--ink`/`--rule` tokens, `%LEMELY_THEME_COLOR%` from `web/vite/themeColor.ts`, `data-shell="portal"` set by `public/shell-init.js`, and React's first commit replaces `#root`'s children. `tests/unit/preMountShell.test.ts` pins it. Run: `npx vitest run tests/unit/preMountShell.test.ts tests/unit/loadingTiers.test.ts` → PASS. Set ledger rows `no-pre-mount-shell` and `T2` to `already-fixed` with evidence `web/index.html pre-mount shell + vite/preMountShell.ts + tests/unit/preMountShell.test.ts present on develop c70dd38d`. What the dossier's T2 still lacks on cold load is the *student shell* (row `student-shell-blanks-on-cold-load`, Task 1) — that row stays `pending`.
- [ ] **Step 3: Re-verify `x-motion-reduced-motion-e2e-narrow-scope`.** Ledger row 231 is `pending`, evidence column says "split: B7 adds e2e half, C4 adds unit half (assigned here to B7)". `web/e2e/reduced-motion.spec.ts` exists and covers only `.lm-screen` + one `Button` on `/teacher/schemes`. Task 12 broadens it; leave `pending`.
- [ ] **Step 4: Commit** `docs(ledger): re-verify Phase B rows already landed on develop (B0)`.

---

### Task 1 (B1): Route skeletons, student shell on cold load, iOS splash screens

**Files:**
- Modify: `web/src/lib/meta/documentMeta.ts:53` (`PageMeta` gains `skeleton?: SkeletonShape`)
- Create: `web/src/components/ui/route-skeleton.tsx` (`RouteSkeleton`)
- Modify: `web/src/components/ui/state-views.tsx:405-420` (`RouteFallback` gains `skeleton?: ReactNode` rendered in the tier-2 slot when `frame === "content"`)
- Modify: `web/src/routes.tsx` — every `fallback={<RouteFallback className="p-8" frame="standalone" />}` (15 sites at `c70dd38d`: `/login:332`, `/signup:393`, `/signup/student:434`, `/signup/teacher:450`, `/signup/parent:479`, `/verify-email:506`, `/verify-email/:token:525`, `/reset:544`, `/reset/:token:563`, `/join:588`, `/join/:code:606`, `/settings/devices:627`, `/settings/notifications:649`, `/settings/profile:667`, `/settings/install:685`) → `fallback={<RouteSkeleton />}`; every child route `handle` gains `skeleton`
- Modify: portal Outlet Suspense sites — `web/src/portals/student/index.tsx:777`, `teacher/index.tsx:593`, `admin/index.tsx:382`, `parent/index.tsx:355`, `marketing/index.tsx:228,258` → `fallback={<RouteSkeleton />}`; child route handles in `student/index.tsx:803-920` and `teacher/index.tsx:617-695` gain `skeleton`
- Modify: `web/src/portals/student/index.tsx:700-702` and `teacher/index.tsx:532-534` — the `isPending` early return that blanks the whole shell
- Create: `web/vite/splashScreens.ts` (`SPLASHES`, `splashLinkTags()`), `web/vite/splashLinks.ts` (Vite HTML transform resolving `%LEMELY_SPLASH_LINKS%`, same shape as `themeColor.ts`)
- Modify: `web/scripts/generate_icons.mjs:69-90` (import `SPLASHES`, render each with the mark on `PAPER`), `web/index.html:26` (add `%LEMELY_SPLASH_LINKS%` after `apple-touch-icon`), `web/vite.config.ts` (register the transform; add `"splash/**"` to the single `globIgnores` array — never a second key), `web/package.json` (`icons` script unchanged; splash PNGs land in `public/splash/`)
- Modify: `DESIGN.md` §12 (route skeleton rule: every route declares `handle.skeleton`; `practice-create-spinner-acceptable-but-note`: the practice "Create" button's `loading` prop is the correct treatment for a sub-second mutation and stays — no code change)
- Test: `web/tests/unit/routeSkeleton.test.ts` (new), `web/tests/unit/loadingTiers.test.ts:50-63` (rewrite the "every top-level RouteFallback is standalone" block — it asserts `RouteFallback` call sites exist in `routes.tsx`, which becomes false), `web/tests/unit/splashScreens.test.ts` (new), `web/tests/unit/checkNativeInvariants.test.ts` (extend)

**Interfaces:**
- Produces: `type SkeletonShape = "page-header" | "card-grid" | "list" | "standalone"` and `PageMeta.skeleton?: SkeletonShape` (`documentMeta.ts`); `skeletonForMatches(matches: readonly { handle?: unknown }[]): SkeletonShape` (pure, exported from `route-skeleton.tsx`; deepest match wins; default `"page-header"` inside a portal, `"standalone"` when no match declares one and the route is top-level); component `RouteSkeleton()` (no props; calls `useMatches()`, renders `<RouteFallback frame={shape === "standalone" ? "standalone" : "content"} skeleton={<PageHeaderSkeleton/> | <CardGridSkeleton/> | <ListSkeleton/>} />`); `SPLASHES: readonly { device: string; width: number; height: number; dpr: 2 | 3 }[]` and `splashLinkTags(): string` (`vite/splashScreens.ts`).
- Consumed by: Task 2 (root layout route keeps `RouteSkeleton`), Task 12 (skeleton visible before content on a throttled load is an optional e2e assertion), `check-native-invariants.mjs` `--phase-b` routes check.

**Skills:** `pwa-expert`, `react-performance-optimizer`, `technical-writer`.

**Behaviours:**
1. `routes.tsx` contains zero `fallback={<RouteFallback`; every `Suspense` fallback in `routes.tsx` and the six portal/marketing Outlet sites is `<RouteSkeleton />`. `RouteFallback`'s tier-2 wrapper (`state-views.tsx:417`) carries `data-tier="skeleton"` so Task 12 can observe it.
2. Every route object with an `element` declares `handle.skeleton`: `"standalone"` for auth/settings/misc top-level routes; `"card-grid"` for `/student` index (Overview), `/student/classes`, `/teacher` index, `/teacher/classes`, `/teacher/quizzes`, `/parent` index; `"list"` for notifications, announcements, review queue, at-risk, schemes, friends, standings, flashcards decks; `"page-header"` for everything else (subject, result, correct, plan, practice, placement, grading, review item, student detail, class detail, profile, parents).
3. `StudentLayout` and `TeacherLayout` no longer return a bare `<RouteFallback className="p-8" />` while their profile/classes query is pending: they render the full shell (Sidebar, NavDrawer, Header) with `<RouteSkeleton />` in the `<main>` slot; `studentOnboardingRedirect` is still evaluated once the query resolves (unchanged function, `tests/unit/onboardingGate.test.ts` untouched).
4. `SPLASHES` covers, portrait and landscape each: iPhone 15 Pro Max/14 Pro Max (430×932@3), 15 Pro/15/14 Pro (393×852@3), 14/13/12 (390×844@3), 14 Plus/13 Pro Max (428×926@3), 13 mini/12 mini (375×812@3), 11 Pro Max/XS Max (414×896@3), 11/XR (414×896@2), 8 Plus (414×736@3), 8/SE (375×667@2), iPad Pro 12.9 (1024×1366@2), iPad Pro 11 (834×1194@2), iPad Air 10.9 (820×1180@2), iPad 10.2 (810×1080@2), iPad mini 8.3 (744×1133@2), iPad Pro 10.5 (834×1112@2), iPad 9.7 (768×1024@2). Pixel size = device × dpr, swapped for landscape. `splashLinkTags()` emits one `<link rel="apple-touch-startup-image" media="screen and (device-width: Wpx) and (device-height: Hpx) and (-webkit-device-pixel-ratio: R) and (orientation: portrait|landscape)" href="/splash/<px-w>x<px-h>.png">` per entry.
5. Splash PNGs: mark centred at `SQUARE_SCALE` of the *shorter* side × 0.28 on a `PAPER` field (read `generate_icons.mjs`'s `ogCard` composite for the sharp pattern); files are ignored by the SW precache (`globIgnores`) and by git only if the repo's `.gitignore` already ignores generated icons — it does not (icons are checked in; see the script header) — so commit them.
6. DESIGN.md §12 gains the two notes in Files above.

- [ ] **Step 1: Re-grep every line reference above; record actual lines in the commit body.** `git grep -n 'fallback={<RouteFallback' web/src` and `git grep -n 'isPending) {' web/src/portals/*/index.tsx`.
- [ ] **Step 2: Write failing tests.** `routeSkeleton.test.ts`: (a) `skeletonForMatches([{handle:{skeleton:"list"}}, {handle:{title:"x"}}])` → `"list"`; `[]` → `"standalone"`; portal chain without a declaration → `"page-header"`; (b) source text of `routes.tsx` (via `stripComments`) contains zero `<RouteFallback` and ≥15 `<RouteSkeleton`; in each of `routes.tsx`, `student/index.tsx`, `teacher/index.tsx`, `parent/index.tsx`, `admin/index.tsx` the count of route objects with a `path:` (plus `index: true` entries) equals the count of `skeleton:` declarations — pathless layout routes (Task 2's `RootOutlet`, Task 3's `ScreenFrame` group) have no `path:` and are exempt; (c) `student/index.tsx` and `teacher/index.tsx` contain zero `<RouteFallback`; (d) `state-views.tsx` contains `data-tier="skeleton"`. `loadingTiers.test.ts`: replace the two assertions at :53-62 with "routes.tsx has zero `<RouteFallback`" and "`route-skeleton.tsx` passes `frame="standalone"` when the shape is standalone". `splashScreens.test.ts`: every entry appears in `splashLinkTags()` twice (portrait, landscape); hrefs unique; `width*dpr`/`height*dpr` integers; `index.html` contains `%LEMELY_SPLASH_LINKS%`; `vite.config.ts` `globIgnores` array contains `"splash/**"` and the string `globIgnores` appears exactly once. `checkNativeInvariants.test.ts`: fixture with `<RouteSkeleton` and no `<RouteFallback` passes the `[phase-b] routes.tsx` check.
- [ ] **Step 3: Run** `npx vitest run tests/unit/routeSkeleton.test.ts tests/unit/loadingTiers.test.ts tests/unit/splashScreens.test.ts tests/unit/checkNativeInvariants.test.ts` → FAIL on every new assertion.
- [ ] **Step 4: Implement behaviours 1–6; DESIGN.md §12 first.** Then `npm run icons` and check `ls public/splash | wc -l` = 32.
- [ ] **Step 5: Run** the four test files → PASS; `npm run typecheck && npm run lint && npm run build`; `node scripts/check-native-invariants.mjs --phase-b` → the routes check passes, the body-overflow check still fails (Task 2 owns it; paste the output in the commit body); `grep -c 'splash/' dist/sw.js` → 0; `grep -c 'apple-touch-startup-image' dist/index.html` → 32.
- [ ] **Step 6: Update ledger rows** route-fallback-not-a-skeleton, student-shell-blanks-on-cold-load, no-ios-splash-screens, practice-create-spinner-acceptable-but-note → `done B1` (no-pre-mount-shell and T2 are `already-fixed` from Task 0).
- [ ] **Step 7: Commits** (two): `feat(web): route-declared skeletons and student shell on cold load (B1)`, `feat(pwa): iOS splash screen matrix generated from the mark (B1)`.

---

### Task 2 (B2a): Dialog history trap, shared scroll lock, ScrollRestoration, post-login return, BackControl

**Files:**
- Create: `web/src/lib/nav/dialogHistory.ts` (pure `nextDialogHistoryAction`), `web/src/lib/nav/useDialogHistory.ts` (hook), `web/src/lib/scrollLock.ts` (`lockScroll(): () => void`), `web/src/lib/nav/scrollRestorationKey.ts` (`TAB_ROOTS`, `scrollRestorationKey`), `web/src/lib/nav/backTarget.ts` (pure `backTarget`), `web/src/lib/auth/postLoginTarget.ts` (pure `postLoginTarget`), `web/src/components/ui/back-control.tsx` (`BackControl`), `web/src/components/root-outlet.tsx` (`RootOutlet`: `<Outlet/>` + `<ScrollRestoration getKey={scrollRestorationKey}/>`)
- Modify: `web/src/components/ui/modal.tsx:87-114` (scroll lock → `lockScroll()`; add `useDialogHistory`), `web/src/components/ui/nav-drawer.tsx:85-108` (same), `web/src/components/ui/confirm-modal.tsx:74-80` (no change to props; its `dismissible={false}` is what drives the re-push branch)
- Modify: `web/src/routes.tsx:307` — wrap `appRoutes` in one pathless layout route `{ element: <RootOutlet />, children: [...existing top-level routes] }` with **no** `errorElement` on the wrapper (every child keeps its own `errorElement`, so bubbling is unchanged; `App.tsx`'s `applyFrom` maps every match's handle and tolerates the extra parent)
- Modify: `web/src/lib/auth/RequireAuth.tsx` (re-grep `Navigate to=`; add `state={{ from: location.pathname + location.search }}` beside the existing `?next=`), `web/src/portals/auth/Login.tsx:133,152` (`postLoginTarget({ next, from: location.state?.from, role })`)
- Modify: `web/src/index.css` `@theme` block near :415 — add `--breakpoint-sidebar: 820px` (the `sidebar:` variant; Task 4 migrates every `min-[820px]`/`md:` sidebar site onto it and adds the width/height tokens)
- Modify: `web/src/portals/student/index.tsx:550-582` (`Header`: render `<BackControl fallback="/student" className="sidebar:hidden" />` before `Breadcrumbs` when `trail.length > 1`)
- Modify: `web/scripts/check-native-invariants.mjs:530-545,571` — the two `[phase-b]` checks run unconditionally; `--phase-b` flag removed
- Test: `web/tests/unit/dialogHistory.test.ts`, `web/tests/unit/scrollLock.test.ts`, `web/tests/unit/scrollRestorationKey.test.ts`, `web/tests/unit/backTarget.test.ts`, `web/tests/unit/postLoginTarget.test.ts` (all new, pure), `web/tests/unit/navigationModel.test.ts` (new, source text), `web/tests/unit/checkNativeInvariants.test.ts` (extend), `web/tests/unit/requireAuth.test.ts` (extend if it reads source)

**Interfaces:**
- Produces: `type DialogHistoryEvent = "open" | "close" | "popstate"`; `nextDialogHistoryAction(input: { event: DialogHistoryEvent; dismissible: boolean; hasEntry: boolean }): "push" | "pop" | "repush" | "closeOnly" | "none"` — open+!hasEntry → `push` (both dismissible and not); close+hasEntry → `pop`; popstate+hasEntry+dismissible → `closeOnly`; popstate+hasEntry+!dismissible → `repush`; else `none`. `useDialogHistory({ open, dismissible, onClose }: { open: boolean; dismissible: boolean; onClose: () => void }): void` — pushes `navigate(location, { state: { ...location.state, lemelyDialog: true } })`, listens to `popstate`, unwinds with `navigate(-1)` on programmatic close, re-pushes for `ConfirmModal`. `lockScroll(): () => void` — saves `window.scrollY`, sets `position: fixed; top: -<scrollY>px; inset-inline: 0; width: 100%` on `document.body`, returns a release that restores and `window.scrollTo(0, saved)`; reference-counted so two overlays (drawer + modal) release correctly. `TAB_ROOTS: ReadonlySet<string>` (empty in this task; Task 4 fills it) and `scrollRestorationKey(location: { pathname: string; key: string }, tabRoots?: ReadonlySet<string>): string` (pathname when it is a tab root, else `location.key`). `backTarget(historyIdx: number | null | undefined, fallback: string): { kind: "history" } | { kind: "route"; to: string }` (`idx` > 0 → history; 0/null/undefined → route). `BackControl({ fallback, label = "Back", className }: { fallback: string; label?: string; className?: string })` — a `Button variant="ghost" size="sm"` with `ArrowLeft`, calls `navigate(-1)` or `navigate(fallback, { replace: true })` per `backTarget(window.history.state?.idx, fallback)`. `postLoginTarget({ next, from, role }: { next: string | null; from: unknown; role: string }): string` — `safeNextPath(next) ?? (safeNextPath(typeof from === "string" ? from : null) that starts with portalPathForRole(role)) ?? portalPathForRole(role)`.
- Consumed by: Task 3 (`isClosing` keeps `useDialogHistory`/`lockScroll` alive), Task 4 (`TAB_ROOTS`, `BackControl` gets `viewTransition`), Task 6 (NavDrawer drag-dismiss calls the same `onClose`), Task 12 (e2e: back dismisses drawer/modal, ConfirmModal survives, scroll restored, deep-link return).

**Skills:** `mobile-ux-optimizer`, `react-best-practices`.

**Behaviours:**
1. Opening a `Modal` (`dismissible` true or false) or a `NavDrawer` pushes one history entry with `state.lemelyDialog === true`; the URL is unchanged.
2. Browser back with a dismissible overlay open closes it (via `onClose`) and does not navigate away; browser back with `ConfirmModal` open re-pushes the entry and does not call `onCancel`.
3. Programmatic close (`onClose`/`onCancel`/scrim/Escape) unwinds the pushed entry with `navigate(-1)` exactly once; the popstate that follows is ignored (guard by a ref set before `navigate(-1)`).
4. `Popover` is untouched (design decision: no history entries for popovers).
5. Neither `modal.tsx` nor `nav-drawer.tsx` contains `document.body.style.overflow`; both call `lockScroll()` while `open`.
6. `<ScrollRestoration/>` is mounted once inside the router (`RootOutlet`), never in `main.tsx` (it must render inside a data-router route); POP restores, PUSH/REPLACE scrolls to top.
7. `RequireAuth` redirect carries `state.from`; `Login` lands the reader on `postLoginTarget(...)` and a `from` outside the role's portal is ignored.
8. `check-native-invariants.mjs` has 20 unconditional checks; `npm run lint` passes.

- [ ] **Step 1: Re-grep** `document.body.style.overflow` (2 files), `Navigate to=` in `RequireAuth.tsx`, `navigate(next ??` in `Login.tsx`, `createBrowserRouter(appRoutes)` in `App.tsx`; read `routes.tsx:241-310` (the errorElement rationale) before adding the wrapper route.
- [ ] **Step 2: Failing tests.** Pure: every branch of `nextDialogHistoryAction`, `scrollRestorationKey` (tab root → pathname; else key), `backTarget` (idx 3 → history; 0/undefined/null → route), `postLoginTarget` (next wins; `from` inside portal honoured; `from` outside portal → home; garbage → home). `scrollLock.test.ts`: implement `lockScroll` against an injected `{ body: { style }, scrollY, scrollTo }` document-like parameter (`lockScroll(doc = window)` default) so the pure logic runs in Node: saves/restores, ref-count 2 → release once keeps lock. `navigationModel.test.ts` (source text): `modal.tsx` and `nav-drawer.tsx` import `useDialogHistory` and `lockScroll` and contain no `body.style.overflow`; `routes.tsx` contains `<RootOutlet` exactly once and `RootOutlet` appears before the first `path:`; `root-outlet.tsx` contains `<ScrollRestoration`; `main.tsx` does not; `RequireAuth.tsx` contains `state={{ from`; `Login.tsx` imports `postLoginTarget`; `student/index.tsx` contains `<BackControl`. `checkNativeInvariants.test.ts`: `runChecks({...})` with no `phaseB` argument includes both former `[phase-b]` checks (rename them, drop the prefix).
- [ ] **Step 3: Run** `npx vitest run tests/unit/dialogHistory.test.ts tests/unit/scrollLock.test.ts tests/unit/scrollRestorationKey.test.ts tests/unit/backTarget.test.ts tests/unit/postLoginTarget.test.ts tests/unit/navigationModel.test.ts tests/unit/checkNativeInvariants.test.ts` → FAIL.
- [ ] **Step 4: Implement behaviours 1–8.**
- [ ] **Step 5: Run** the seven test files → PASS; `npm run typecheck && npm run lint && npm run build`; `node scripts/check-native-invariants.mjs` → `all 20 check(s) passed`. Manual: `npm run dev`, open the student drawer on a 375px viewport, press browser back → drawer closes, URL unchanged; open "Delete deck" on `/student/flashcards/<code>`, press back → dialog stays. Record both in the commit body.
- [ ] **Step 6: Ledger:** nav-1, nav-3, nav-4, nav-6, modal-drawer-scroll-lock-fragile → `done B2`. `gesture-standalone-pwa-no-back-replacement` stays `pending` until Task 4's edge-swipe lands (BackControl is half of it).
- [ ] **Step 7: Commits** (two): `feat(web): dialogs push a history entry, shared scroll lock, ScrollRestoration (B2)`, `feat(web): post-login return target and BackControl in student sub-screens (B2)`.

---

### Task 3 (B2b): Screen entrance on the Outlet, View Transitions, exit animations, DESIGN.md §9

**Files:**
- Create: `web/src/components/ui/screen-outlet.tsx` (`ScreenOutlet`), `web/src/lib/nav/navigationDirection.ts` (pure `navigationDirection`)
- Modify: `web/src/components/root-outlet.tsx` (from Task 2: `RootOutlet` sets `document.documentElement.dataset.direction` from `navigationDirection(useNavigationType())` in a layout effect)
- Modify: portal layouts — `web/src/portals/student/index.tsx:793`, `teacher/index.tsx:607`, `admin/index.tsx:396`, `parent/index.tsx:369` (`<Outlet />` → `<ScreenOutlet />`), `web/src/portals/settings/SettingsFrame.tsx` (wrap its content slot in `ScreenOutlet`'s inner wrapper — it is not an Outlet host; export `ScreenFrame({ children })` from `screen-outlet.tsx` for this and for the auth/marketing/misc top-level routes, which get it via a second pathless layout route in `routes.tsx` grouping every non-portal route)
- Modify: the ~30 screens carrying `className="lm-screen …"` on their root (`git grep -ln 'lm-screen' web/src --include='*.tsx'` — 30 files at `c70dd38d`, `FlashcardReview.tsx` has 4 sites, `Subject.tsx`/`CreateFirstClass.tsx` 2 each): remove the class from the screen root; `PaperResult.tsx:127` keeps its own `lm-screen` (excluded from VT, see Behaviour 6) and its route declares `handle.viewTransition: false`
- Modify: `web/src/lib/meta/documentMeta.ts` (`PageMeta.viewTransition?: false`)
- Modify: `web/src/index.css:1301-1303` (`.lm-screen` rule gains the VT suppression; new `lm-out`, `lm-slide-out-start` keyframes after `lm-slide-in-start:998`; new `::view-transition-old(root)`/`::view-transition-new(root)` rules keyed on `html[data-direction="back"|"forward"]`; reduced-motion block at :1452 gains `::view-transition-group(*), ::view-transition-old(*), ::view-transition-new(*) { animation: none !important }`)
- Modify: `web/src/components/ui/modal.tsx`, `web/src/components/ui/nav-drawer.tsx` (`isClosing` phase), `web/src/components/ui/breadcrumbs.tsx`, `web/src/components/ui/back-control.tsx`, sidebar `NavLink`s in `student/index.tsx:218`, `teacher/index.tsx:155`, `admin/index.tsx:96` (`viewTransition` prop; Task 4's `BottomNav`/`SidebarNav` inherit it via `NavShellItem`)
- Modify: `DESIGN.md` §9.2 (new "Exits" clause) and §9.4 (new "When View Transitions apply" rule)
- Test: `web/tests/unit/navigationDirection.test.ts` (pure), `web/tests/unit/screenEntrance.test.ts` (source text), `web/tests/unit/motionDefaults.test.ts` (extend: `lm-out` uses `--dur-fast` + `--ease-in-soft`; VT rules animate only `transform`/`opacity`), `web/tests/unit/overlayExit.test.ts` (pure `overlayPhase` reducer)

**Interfaces:**
- Produces: `navigationDirection(type: "POP" | "PUSH" | "REPLACE"): "back" | "forward"`; `ScreenOutlet()` renders `<div key={location.key} className="lm-screen" data-screen><Outlet/></div>` and `ScreenFrame({ children })` the same wrapper without the Outlet; `overlayPhase(state: "closed" | "open" | "closing", event: "open" | "close" | "animationend", reducedMotion: boolean): "closed" | "open" | "closing"` (`lib/overlayPhase.ts`; `open→close` yields `closing` unless `reducedMotion`, in which case `closed`; `closing→animationend` yields `closed`; `closing→open` yields `open`). `Modal` and `NavDrawer` keep their public props; internally `mounted = phase !== "closed"`, panel gets class `lm-out`/`lm-slide-out-start` while `closing`, `onAnimationEnd` advances the phase; `useDialogHistory` and `lockScroll` run for `phase !== "closed"`.
- Consumed by: Task 4 (nav items pass `viewTransition`), Task 6 (drawer drag-dismiss ends by calling `onClose`, exit animation then plays), Task 12 (reduced-motion e2e: modal unmounts within one frame; durations ≤10ms).

**Skills:** `react-view-transitions` (consult for the `::view-transition-*` pseudo-element CSS and the directional pattern only — this repo uses react-router's `viewTransition` prop, which drives `document.startViewTransition` natively; do **not** adopt React's experimental `<ViewTransition>` component unless `import { ViewTransition } from "react"` typechecks on the pinned React version — record the check in the commit body), `mobile-ux-optimizer`, `react-best-practices`, `technical-writer`.

**Behaviours:**
1. `.lm-screen` fires on navigation (the wrapper remounts on `location.key`), not on in-screen state swaps; no screen root carries `lm-screen` except `PaperResult.tsx`.
2. Auth, marketing, misc, settings, parent and admin screens get the same entrance as student/teacher screens.
3. Portal-internal navigation via shell links (sidebar, breadcrumbs, BackControl, and Task 4's BottomNav/drawer) passes `viewTransition`; `html[data-direction]` is `"back"` for POP and `"forward"` otherwise; `::view-transition-old(root)` translates ±24px `--lm-dir`-aware over `--dur-base` `--ease-out-soft`, `::view-transition-new(root)` the mirror; `html:active-view-transition .lm-screen { animation: none }` so the two never stack.
4. Links to `/student/result/:paperId` and `CorrectPaper`'s `navigate("/student/result/…")` do not pass `viewTransition`; the result route declares `viewTransition: false` and keeps its celebration entrance.
5. Reduced motion: `::view-transition-*` animations are `none`, and overlays unmount synchronously (`overlayPhase(..., reducedMotion=true)`), using `prefersReducedMotion()` from `lib/celebration.ts:203`.
6. Dismissible overlays play `lm-out` (opacity → 0, translateY 8px) / `lm-slide-out-start` over `--dur-fast` `--ease-in-soft`, then unmount on `animationend`; scroll lock and focus trap stay active during `closing`; a re-open during `closing` snaps back to `open`.
7. DESIGN.md §9.2 "Exits" clause: every dismissible overlay exits over `dur-fast`/`ease-in-soft` and unmounts on `animationend`; §9.4 rule: View Transitions apply only to portal-internal navigation initiated from shell chrome, never to the result reveal, never under reduced motion.

- [ ] **Step 1: Re-grep** `lm-screen` sites (30 files), `<Outlet />` in the four portal layouts, `NavLink` in the three sidebars, `motion-safe:animate-[lm-in` in `modal.tsx:162,175` and `nav-drawer.tsx:144,162`.
- [ ] **Step 2: Failing tests.** `navigationDirection.test.ts`: POP → back, PUSH/REPLACE → forward. `overlayExit.test.ts`: every transition in the Interfaces block. `screenEntrance.test.ts` (source text, `stripComments`): no file under `web/src/portals` except `PaperResult.tsx` contains `"lm-screen`; `screen-outlet.tsx` contains `key={location.key}` and `lm-screen`; each of the four portal layouts contains `<ScreenOutlet` and not `<Outlet />`; `routes.tsx` has a second pathless layout route containing `ScreenFrame`; `breadcrumbs.tsx`, `back-control.tsx` and the three sidebar `NavLink` sites contain `viewTransition`; `PaperResult.tsx`'s route line contains `viewTransition: false`; `index.css` contains `::view-transition-old(root)`, `html:active-view-transition .lm-screen`, and inside the `prefers-reduced-motion` block `::view-transition-group(*)`. `motionDefaults.test.ts`: `@keyframes lm-out` exists, the `.lm-out` rule references `var(--dur-fast)` and `var(--ease-in-soft)`; no VT rule mentions `width`, `height`, `top`, `left`.
- [ ] **Step 3: Run** `npx vitest run tests/unit/navigationDirection.test.ts tests/unit/overlayExit.test.ts tests/unit/screenEntrance.test.ts tests/unit/motionDefaults.test.ts` → FAIL.
- [ ] **Step 4: Implement behaviours 1–7; DESIGN.md first.**
- [ ] **Step 5: Run** the four files plus `tests/unit/navigationModel.test.ts` → PASS; `npm run typecheck && npm run lint && npm run build && npm run check:copy`. If the backend is up: `npx playwright test e2e/reduced-motion.spec.ts` (it reads `.lm-screen` on `/teacher/schemes`, now the outlet wrapper) → PASS; else record "e2e deferred to CI".
- [ ] **Step 6: Ledger:** T1, T3, T4 → `done B2`.
- [ ] **Step 7: Commits** (two): `feat(web): screen entrance on the outlet, View Transitions for shell navigation (B2)`, `feat(web): exit phase for dismissible overlays, DESIGN.md exits clause (B2)`.

---

### Task 4 (B3): Bottom CTA bar, BottomNav tabs, SidebarNav retrofit, edge-swipe-back

**Files:**
- Create: `web/src/lib/gestures/useDragGesture.ts` (created here because this task's edge-swipe is its first consumer; Task 5/6 consume it — spec below is the single source), `web/src/lib/gestures/dragMath.ts` (pure `shouldCommitDrag`, `dragProgress`), `web/src/components/ui/bottom-action-bar.tsx` (`BottomActionBar`), `web/src/lib/nav/tabMemory.ts` (`useTabState`), `web/src/lib/nav/edgeSwipeBack.ts` (pure `edgeSwipeDecision`), `web/src/components/edge-swipe-back.tsx` (`EdgeSwipeBack`, renders nothing)
- Modify: `web/src/components/ui/nav-shells.tsx` — `NavShellItem` gains `to?: string` + `end?: boolean` (rendered with `NavLink viewTransition`, `active` computed by `NavLink`'s `isActive`), keeps `href`/`onClick` for the "More" button; export `SIDEBAR_WIDTH = 252`, `SIDEBAR_BREAKPOINT = 820`, `BOTTOM_NAV_HEIGHT = 56`; `BottomNav` gains `emphasisId?: string` (the "Correct" tab rendered as an accent circle); `SidebarNav` gains `groups?: { label: string; items: NavShellItem[] }[]` for the student portal's grouped nav; comment block at :4-16 rewritten (it describes the retrofit as future work)
- Modify: `web/src/index.css` — beside Task 2's `--breakpoint-sidebar: 820px`: tokens `--sidebar-width: 252px`, `--bottom-nav-height: 56px`; utilities `w-sidebar`, `pb-bottom-nav` (padding-bottom `calc(var(--bottom-nav-height) + env(safe-area-inset-bottom))`)
- Modify: `web/vite/preMountShell.ts` + `web/index.html:205-206,429-430` (`246px`/`820px` literals → `%LEMELY_SIDEBAR_WIDTH%`/`%LEMELY_SIDEBAR_BREAKPOINT%` resolved from the same tokens; `tests/unit/preMountShell.test.ts` extended)
- Modify: `web/src/portals/student/index.tsx` (`Sidebar:476-492` `hidden min-[820px]:flex w-[246px]` → `hidden sidebar:flex w-sidebar`; `NavGroups:366-470` → `SidebarNav groups`; `Header:585` trigger → `sidebar:hidden`; `StudentLayout:715-800` mounts `<BottomNav>` + `<BottomActionBar>` + `<EdgeSwipeBack>`; `<main>` gains `pb-bottom-nav sidebar:pb-8`; `TAB_ROOTS` filled with `/student`, `/student/correct`, `/student/classes`, `/student/profile`, `/teacher`, `/teacher/grading`, `/teacher/review`, `/teacher/classes`), `teacher/index.tsx` (`Sidebar:403-407` `hidden md:flex w-[252px]` → `hidden sidebar:flex w-sidebar`; `SidebarNavItem:141-203` → `SidebarNav`; `TeacherLayout` mounts `BottomNav` + `EdgeSwipeBack`), `admin/index.tsx` (`Sidebar:288-292`, `SidebarNavItem:83-135` → `SidebarNav`; no BottomNav — admin is desktop-first, record the decision)
- Modify: `web/src/components/ui/nav-drawer.tsx:32-40` (comment: the ranking decision is now made — five tabs per the user decision; the drawer stays as the complete list behind "More")
- Modify: `web/src/lib/nav/scrollRestorationKey.ts` (Task 2's `TAB_ROOTS` populated)
- Modify: `DESIGN.md` §15 "Gestures" (edge-swipe-back entry) and a new §15 "Navigation chrome" paragraph (sidebar constants, bottom nav, thumb-zone CTA)
- Test: `web/tests/unit/dragMath.test.ts`, `web/tests/unit/edgeSwipeBack.test.ts`, `web/tests/unit/tabMemory.test.ts` (pure), `web/tests/unit/navShells.test.ts` (new, source text + constants), `web/tests/unit/navigation.test.ts` (extend: every BottomNav `to` is a mounted route), `web/tests/unit/design-tokens.test.ts` (extend: `--breakpoint-sidebar`, `--sidebar-width`, `--bottom-nav-height`), `web/tests/unit/nativeMechanics.test.ts` (extend: no `min-[820px]`/`w-[246px]`/`w-[252px]`/`md:flex` on any `<aside` in `web/src/portals`), `web/tests/unit/preMountShell.test.ts` (extend)

**Interfaces:**
- Produces: `useDragGesture(ref: RefObject<HTMLElement | null>, options: DragGestureOptions): void` where `DragGestureOptions = { axis: "x" | "y"; onCommit: (dx: number, dy: number) => void; onProgress?: (dx: number, dy: number) => void; onCancel?: () => void; commitThreshold?: number /* default 10 */; enabled?: boolean /* default true */; startFilter?: (event: PointerEvent) => boolean; transformTarget?: RefObject<HTMLElement | null> }` — Pointer Events with `setPointerCapture`, all tracking in refs (nothing on the render path), commit when `shouldCommitDrag(dx, dy, axis, commitThreshold)`, imperative `style.transform` on `transformTarget` (or `ref`) during the drag, spring-back on release-without-commit using `transition: transform var(--dur-base) var(--ease-spring)` then cleared on `transitionend`. `shouldCommitDrag(dx: number, dy: number, axis: "x" | "y", threshold = 10): boolean` (`axis x`: `|dx| > threshold && |dx| > 2|dy|`; `axis y`: mirrored). `dragProgress(delta: number, distance: number): number` (0..1 clamped). `edgeSwipeDecision({ startX, dx, dy, viewportWidth, standalone, dir }: { startX: number; dx: number; dy: number; viewportWidth: number; standalone: boolean; dir: 1 | -1 }): "back" | "none"` — `"back"` only when `standalone`, the start point is within 24px of the inline-start edge (`startX <= 24` for `dir 1`, `startX >= viewportWidth - 24` for `dir -1`), and `shouldCommitDrag(dx * dir, dy, "x")` with `dx * dir > 0`. `EdgeSwipeBack()` — standalone-only (`isStandaloneDisplay` from `lib/pwa/useInstallPrompt.ts:92`), installs one `pointerdown` listener on `document`, uses `useDragGesture` on a ref to `document.documentElement`, on commit calls the same `backTarget` logic as `BackControl` (Task 2) with fallback = the portal root. `useTabState<T>(key: string, initial: T): [T, (next: T) => void]` — like `useState`, backed by a module `Map` keyed by `tabRoot(location.pathname) + ":" + key` (persists across tab switches, cleared on sign-out via `clearTabMemory()`); `tabRoot(pathname: string, tabRoots = TAB_ROOTS): string | null` (pure, tested). `BottomActionBar({ to, label, hideOn }: { to: string; label: string; hideOn: string[] })` — fixed above the BottomNav (`bottom: var(--bottom-nav-height)`), `sidebar:hidden`, `Link viewTransition` styled as the primary button, hidden on `hideOn` paths; rendered only by `StudentLayout` on routes whose `handle.primaryAction === "correct"` (Overview, Subject, PaperResult declare it; `PageMeta.primaryAction?: "correct"`). `NavShellItem` (final shape): `{ id: string; label: string; icon: ReactNode; to?: string; end?: boolean; href?: string; onClick?: () => void; active?: boolean; badge?: string | number; prefetch?: () => void }` — `to` wins (NavLink, active from router), `active` only used for `href`/`onClick` items; `prefetch` is declared here and wired by Task 11. `SIDEBAR_WIDTH`, `SIDEBAR_BREAKPOINT`, `BOTTOM_NAV_HEIGHT` (numbers) exported from `nav-shells.tsx`; `design-tokens.test.ts` asserts they equal the `index.css` tokens.
- Consumed by: Task 5 (`useDragGesture`, `shouldCommitDrag`), Task 6 (NavDrawer drag-dismiss), Task 11 (prefetch on the "Correct" tab and `BottomActionBar`), Task 12 (tab targets, edge-swipe is not e2e-testable in Playwright's emulation without standalone mode — documented).

**Skills:** `native-app-designer`, `mobile-ux-optimizer`, `ux-heuristics`, `technical-writer`.

**Behaviours:**
1. Below the `sidebar` breakpoint the student portal shows a five-tab `BottomNav` (Overview `/student` end · Correct `/student/correct` · Classes `/student/classes` · Profile `/student/profile` · More → opens the existing `NavDrawer`); the teacher portal shows Overview `/teacher` end · Grading `/teacher/grading` · Review `/teacher/review` (badge = review count, reuse `_count_review_items`'s client counterpart if a query exists — re-grep `useReviewQueue` usage in `teacher/index.tsx`; if none, no badge, note it) · Classes `/teacher/classes` · More. `aria-current="page"` comes from `NavLink`.
2. `main` carries `pb-bottom-nav` below the breakpoint so the last row is never under the bar; the `lm-safe-bottom` utility from A1 stays on the bar.
3. The three sidebars (student, teacher, admin) render through `SidebarNav`; `nav-shells.tsx` no longer says the retrofit is pending; every sidebar uses `w-sidebar` and `sidebar:flex`, and the pre-mount shell's sidebar uses the same two tokens.
4. Switching tabs and returning restores the previous scroll position (`ScrollRestoration` keyed by pathname for `TAB_ROOTS`) and the previous local state for every screen that adopts `useTabState` (Overview, Classes, Profile, Notifications — apply to each screen's one filter/expanded state; if a screen has none, say so in the commit body).
5. `BottomActionBar` "Correct a paper" appears on Overview/Subject/PaperResult below the breakpoint and never on `/student/correct`; the header CTA at `student/index.tsx:611-618` stays for ≥ breakpoint (`hidden sidebar:inline-flex`).
6. In standalone display mode a drag from the inline-start 24px that commits (`|dx| > 10 && |dx| > 2|dy|`, away from the edge) navigates back (history when `idx > 0`, else the portal root). In a browser tab the listener is not installed.
7. DESIGN.md §15 gestures list gains "edge-swipe-back (standalone only, 24px inline-start edge)"; a "Navigation chrome" paragraph names the constants and the five tabs.

- [ ] **Step 1: Re-grep** `hidden min-[820px]:flex`, `hidden md:flex`, `w-[246px]`, `w-[252px]`, `NavGroups`, `SidebarNavItem`, `min-[820px]:hidden`, `_count_review_items`; read `nav-drawer.tsx:25-50` and `nav-shells.tsx` whole.
- [ ] **Step 2: Failing tests.** `dragMath.test.ts`: `shouldCommitDrag(11, 5, "x")` true, `(11, 6, "x")` false, `(10, 0, "x")` false, axis `y` mirrored; `dragProgress` clamps. `edgeSwipeBack.test.ts`: browser tab → none; standalone + startX 30 → none; standalone + startX 12 + dx 40 dy 5 → back; RTL (`dir -1`, startX = width-10, dx -40) → back; dx toward the edge → none. `tabMemory.test.ts`: `tabRoot("/student/classes")` → `/student/classes`, `tabRoot("/student/subject/9709")` → null, `tabRoot("/teacher/review/abc")` → null. `navShells.test.ts`: `SIDEBAR_WIDTH === 252`, `SIDEBAR_BREAKPOINT === 820`, `BOTTOM_NAV_HEIGHT === 56`; source of `nav-shells.tsx` imports `NavLink` and passes `viewTransition`; the three portal `index.tsx` import `SidebarNav`; `student/index.tsx` and `teacher/index.tsx` import `BottomNav` and contain the five labels in order (regex over the stripped source: `"Overview"[\s\S]*"Correct"[\s\S]*"Classes"[\s\S]*"Profile"[\s\S]*"More"`); `nav-drawer.tsx` no longer contains `stays in the kit unused`. `navigation.test.ts` (extend, it already checks nav entries against mounted routes): every `to` in the two BottomNav item arrays resolves to a mounted route. `design-tokens.test.ts` + `nativeMechanics.test.ts` + `preMountShell.test.ts` as listed in Files.
- [ ] **Step 3: Run** `npx vitest run tests/unit/dragMath.test.ts tests/unit/edgeSwipeBack.test.ts tests/unit/tabMemory.test.ts tests/unit/navShells.test.ts tests/unit/navigation.test.ts tests/unit/design-tokens.test.ts tests/unit/nativeMechanics.test.ts tests/unit/preMountShell.test.ts` → FAIL.
- [ ] **Step 4: Implement behaviours 1–7** in this order: tokens + constants → `useDragGesture` → SidebarNav retrofit → BottomNav → BottomActionBar → tab memory → EdgeSwipeBack → DESIGN.md.
- [ ] **Step 5: Run** the eight files → PASS; `npm run typecheck && npm run lint && npm run build && npm run check:copy`; `node scripts/check-native-invariants.mjs` → all pass (the `lm-nav-chrome` check at :515 must still find every top-level nav — `BottomNav` already carries it). Manual at 375px: tabs navigate, "More" opens the drawer, CTA bar visible on Overview, hidden on Correct; record in commit body.
- [ ] **Step 6: Ledger:** nav-2, nav-5, TT-1, x-navshells-dead-code, x-ia-navshells-unused-abstraction, x-ia-sidebar-cross-portal-mismatch → `done B3`; `gesture-standalone-pwa-no-back-replacement` → `done B3` (BackControl from Task 2 + edge-swipe here).
- [ ] **Step 7: Commits** (four): `feat(web): sidebar tokens and SidebarNav retrofit across student, teacher, admin (B3)`, `feat(web): useDragGesture hook (B3, shared with B4)`, `feat(web): BottomNav tabs, bottom CTA bar, per-tab scroll and state memory (B3)`, `feat(pwa): edge-swipe-back in standalone mode (B3)`.

---

### Task 5 (B4a): Gesture hooks — pull-to-refresh, long-press

**Files:**
- Create: `web/src/lib/gestures/usePullToRefresh.ts`, `web/src/lib/gestures/pullMath.ts` (pure `pullState`), `web/src/lib/gestures/useLongPress.ts`, `web/src/lib/gestures/longPressMath.ts` (pure `longPressDecision`), `web/src/components/ui/pull-indicator.tsx` (`PullIndicator`)
- Test: `web/tests/unit/pullMath.test.ts`, `web/tests/unit/longPressMath.test.ts` (pure), `web/tests/unit/gestureHooks.test.ts` (source text: refs only, no `useState` for pointer coordinates)

**Interfaces:**
- Produces: `usePullToRefresh(ref: RefObject<HTMLElement | null>, options: { onRefresh: () => Promise<unknown>; threshold?: number /* 72 */; enabled?: boolean }): { pulling: boolean; refreshing: boolean }` — sets `overscroll-behavior-y: contain` on `ref.current` while mounted, only engages when `ref.current.scrollTop === 0` (or `window.scrollY === 0` when `ref` is the document), uses `useDragGesture` with `axis: "y"`, imperative translate on the indicator, calls `onRefresh` once when released past `threshold`, resets on settle. `pullState(dy: number, threshold: number, atTop: boolean): { progress: number; armed: boolean }`. `useLongPress(handlers: { onLongPress: (event: PointerEvent) => void; holdMs?: number /* 500 */; moveTolerance?: number /* 10 */ }): { onPointerDown; onPointerMove; onPointerUp; onPointerCancel; onContextMenu }` — spread onto the row; fires once after `holdMs` without movement beyond `moveTolerance`; cancels on move/up/cancel; suppresses the native context menu only while a press is armed; ignores presses that start on an interactive descendant (`closest("button, a, input, textarea, select, [role=radio], [role=checkbox]")`). `longPressDecision({ elapsedMs, movedPx, holdMs, moveTolerance, startedOnInteractive }): "fire" | "wait" | "cancel"`. `PullIndicator({ progress, refreshing }: { progress: number; refreshing: boolean })` — a 44px circle with the `lm-spin` keyframe at :1017 when refreshing.
- Consumed by: Task 6 (Notifications, Announcements, Overview pull-to-refresh; QuestionRow, notification row, deck row long-press).

**Skills:** `mobile-ux-optimizer`, `native-app-designer`, `error-handling-patterns`.

- [ ] **Step 1: Re-grep** `useDragGesture` signature (Task 4) and `lm-spin`.
- [ ] **Step 2: Failing tests.** `pullMath.test.ts`: not at top → progress 0/unarmed regardless of dy; dy 36/72 → 0.5 unarmed; dy 80/72 → 1 armed; negative dy → 0. `longPressMath.test.ts`: 499ms/0px → wait; 500ms/0px → fire; 200ms/11px → cancel; startedOnInteractive → cancel. `gestureHooks.test.ts`: `usePullToRefresh.ts`/`useLongPress.ts`/`useDragGesture.ts` sources contain no `useState<` whose name includes `x`, `y`, `dx`, `dy` (regex `useState[^\n]*\b(dx|dy|startX|startY)\b` → 0 matches) and each contains `useRef`.
- [ ] **Step 3: Run** `npx vitest run tests/unit/pullMath.test.ts tests/unit/longPressMath.test.ts tests/unit/gestureHooks.test.ts` → FAIL.
- [ ] **Step 4: Implement.**
- [ ] **Step 5: Run** → PASS; `npm run typecheck && npm run lint && npm run build`.
- [ ] **Step 6: Ledger:** none (hooks only; rows close in Task 6).
- [ ] **Step 7: Commit** `feat(web): pull-to-refresh and long-press gesture hooks (B4)`.

---

### Task 6 (B4b): Gesture wiring — flashcards, drawer, pull-to-refresh, quiz, long-press menus

**Files:**
- Modify: `web/src/portals/student/screens/flashcards/FlashcardReview.tsx:93-105,273-340` (card panel gets `useDragGesture`; swipe up/right ≥60px reveals; once revealed, swipe left ≥80px → `grade("again")`, right ≥80px → `grade("good")`; buttons at :316,:334 and the keyboard handler at :107-129 untouched)
- Modify: `web/src/components/ui/nav-drawer.tsx` (panel `useDragGesture` axis `x`, `startFilter` excludes the scrollable list mid-scroll, commit when `dx * dir < 0` past 40% of panel width → `onClose()`; RTL via `--lm-dir` read from `getComputedStyle(document.documentElement).getPropertyValue("--lm-dir")`)
- Modify: `web/src/portals/student/screens/Notifications.tsx`, `Announcements.tsx` (:240 `query`), `Overview.tsx` (:24 `useOverview`) — a `ref` on the screen's scroll container (the `ScreenOutlet` wrapper is not it; use the screen root and `document` scrolling) with `usePullToRefresh({ onRefresh: () => query.refetch() })` and `<PullIndicator>` at the top
- Modify: `web/src/components/quiz/QuizTaker.tsx:216,406-430,514-515,647-656` (question panel `useDragGesture` axis `x`; `startFilter` rejects targets matching `[role="radio"], textarea, input, button, a`; on commit `await flushPendingSaves()` then `setIndex(clampQuestionIndex(i ± 1, total))`; `enabled: !(remaining !== null && remaining <= 60)`)
- Modify: `web/src/components/ui/question-row.tsx:48-110` (row gets `useLongPress` → controlled `Popover` (`open`/`onOpenChange`) anchored to the row with items "Practice this topic" (`Link` to `/student/practice/:subjectCode?topic=` — only when the row has `subjectCode`/`topicId` props; at `c70dd38d` `QuestionRowProps` has neither: add optional `practiceHref?: string` and `onShare?: () => void` props; the item renders only when provided; `PaperResult.tsx` passes `practiceHref` when it can build one from its own data and `onShare` from Task 7's `shareResult`), `web/src/portals/student/screens/Notifications.tsx:170-185` (row long-press → Popover "Mark read" calling `markRead.mutate(notification.notificationId)`), `web/src/portals/student/screens/flashcards/FlashcardDecks.tsx:600-640` (deck row long-press → Popover "Delete" → the existing `setDeckPendingDelete` confirm flow; **no "Rename"**: `lemely/web/routers/flashcards.py` has `DELETE /decks/{id}` and `PATCH /cards/{id}` but no deck rename endpoint, and `useFlashcardApi.ts` has no `useUpdateDeck` — omit and say so in the commit body)
- Modify: `DESIGN.md` §15 "Gestures": the shipped list (flashcard reveal/grade swipes, drawer drag-dismiss, pull-to-refresh on the three list screens, quiz page-turn with its exclusions and last-60s lock, long-press menus on the three rows) and the affirmed override for `gesture-no-long-press-and-thats-fine`: long-press is shipped only where a row has secondary actions the dossier named; the one place the dossier suggested and the user declined stays without it — record which (read the dossier article for the finding id in the ledger's source HTML if needed; otherwise state "no additional long-press targets beyond the three rows")
- Test: `web/tests/unit/flashcardSwipe.test.ts` (pure `flashcardSwipeAction`), `web/tests/unit/quizSwipe.test.ts` (pure `quizSwipeAllowed`), `web/tests/unit/gestureWiring.test.ts` (source text), `web/tests/unit/flashcards.test.ts` and `tests/unit/quizTaker.test.ts` (untouched — must still pass)

**Interfaces:**
- Produces: `flashcardSwipeAction({ dx, dy, revealed }: { dx: number; dy: number; revealed: boolean }): "reveal" | "again" | "good" | "none"` (`lib/flashcardSwipe.ts`: unrevealed → up ≤ -60 or right ≥ 60 → reveal; revealed → left ≤ -80 → again, right ≥ 80 → good; else none). `quizSwipeAllowed({ remainingSeconds, targetTag, targetRole }: { remainingSeconds: number | null; targetTag: string; targetRole: string | null }): boolean` (`lib/quizSwipe.ts`). `QuestionRowProps` gains `practiceHref?: string; onShare?: () => void`.
- Consumes: Task 4 `useDragGesture`/`shouldCommitDrag`, Task 5 hooks, Task 2 `onClose` contract (drawer drag-dismiss ends in `onClose`, which unwinds history and plays Task 3's exit).

**Skills:** `mobile-ux-optimizer`, `native-app-designer`, `error-handling-patterns`, `technical-writer`.

**Behaviours:** every visible button, link and keyboard shortcut keeps working exactly as before; the gestures are additive. Flashcard swipe never grades before reveal. Quiz swipe never fires from a radio/textarea/input, always flushes the 600ms autosave first, and is disabled when `remaining <= 60`. Drawer drag-dismiss is RTL-aware. Pull-to-refresh only engages at scroll top and calls `refetch` once per pull. Long-press opens the existing `Popover` with only real actions.

- [ ] **Step 1: Re-grep** every line above; read `Popover`'s controlled API (`popover.tsx:40-60`) and `QuestionRowProps:33-47`.
- [ ] **Step 2: Failing tests.** `flashcardSwipe.test.ts` every branch; `quizSwipe.test.ts`: 59s → false, 61s → true, null → true, radio/textarea/input/button → false. `gestureWiring.test.ts` (source text): `FlashcardReview.tsx` imports `useDragGesture` and `flashcardSwipeAction` and still contains `Reveal answer` and all four `GRADE_BUTTONS` labels and `isRevealKey`; `nav-drawer.tsx` contains `useDragGesture` and `--lm-dir`; `QuizTaker.tsx` contains `quizSwipeAllowed`, `flushPendingSaves()` inside the commit handler (regex: `onCommit[\s\S]{0,400}flushPendingSaves`), and still contains `Previous` and `Next`; `Notifications.tsx`, `Announcements.tsx`, `Overview.tsx` import `usePullToRefresh`; `question-row.tsx`, `Notifications.tsx`, `FlashcardDecks.tsx` import `useLongPress` and `Popover`; `FlashcardDecks.tsx` contains no `Rename`.
- [ ] **Step 3: Run** `npx vitest run tests/unit/flashcardSwipe.test.ts tests/unit/quizSwipe.test.ts tests/unit/gestureWiring.test.ts tests/unit/flashcards.test.ts tests/unit/quizTaker.test.ts` → FAIL on the new files.
- [ ] **Step 4: Implement; DESIGN.md last.**
- [ ] **Step 5: Run** → PASS; `npm run typecheck && npm run lint && npm run build && npm run check:copy`. Manual on a touch-emulated 375px viewport: each gesture once; each existing button once; record in the commit body.
- [ ] **Step 6: Ledger:** gesture-flashcard-no-swipe, gesture-navdrawer-no-swipe-dismiss, gesture-no-pull-to-refresh, gesture-quiztaker-no-swipe-honest-tradeoff, gesture-no-long-press-and-thats-fine → `done B4` (T8 is a tell with no ledger row; name it in the commit body).
- [ ] **Step 7: Commits** (three): `feat(web): flashcard swipe reveal and grade, drawer drag-dismiss (B4)`, `feat(web): pull-to-refresh on notifications, announcements, overview; quiz page-turn swipe (B4)`, `feat(web): long-press menus on question, notification and deck rows (B4)`.

---

### Task 7 (B5a): Optimistic flashcard grading, haptics, Wake Lock, Web Share, Badging, landscape guidance

**Files:**
- Modify: `web/src/lib/hooks/useFlashcardApi.ts:195-212` (`useReviewCard` gains `retry: 3`, `retryDelay: (attempt) => Math.min(1000 * 2 ** attempt, 8000)`), `web/src/portals/student/screens/flashcards/FlashcardReview.tsx:86-106,195-230,340-360` (optimistic advance; `failedGrades` banner; summary from settled `results` only)
- Create: `web/src/lib/flashcardSession.ts` (pure `applyGradeOutcome`), `web/src/lib/haptics.ts`, `web/src/lib/wakeLock.ts` (`useWakeLock`), `web/src/lib/share.ts` (`shareResult`, `canWebShare`), `web/src/lib/badging.ts` (`setAppBadge`), `web/src/components/badge-sync.tsx` (`BadgeSync`, renders nothing)
- Modify: `web/src/components/ui/confirm-modal.tsx:92` (`onClick={() => { haptic("tap"); onConfirm() }}`), `FlashcardReview.tsx` session end (`haptic("success")` once when `finished` becomes true), `web/src/components/CameraCapture.tsx:150-190` (`useWakeLock(phase === "live" && started)`; landscape hint element `hidden landscape:pointer-coarse:flex` — Tailwind 4's built-in `landscape:` variant, no arbitrary media query), `web/src/portals/student/screens/PaperResult.tsx:288-300` (header "Share" secondary button when `paperId` is present and not `live`; calls `shareResult({ url: location.origin + "/student/result/" + paperId, title, text })`), `web/src/portals/student/index.tsx`, `teacher/index.tsx`, `parent/index.tsx` (mount `<BadgeSync />` inside the authenticated layout — `useNotificationCounts` hits an auth-gated endpoint, so it must not mount in `main.tsx`), `web/src/portals/student/screens/Notifications.tsx` (on mount `setAppBadge(0)`), `web/src/sw.ts:264-292` + `web/src/sw/` push decision module (re-grep `decidePushNotification`; `PushNotificationContent` gains `unread?: number`; the page-side handshake reply includes the cached `NotificationCounts.unread`; `handlePush` calls `self.navigator.setAppBadge?.(unread)` when present)
- Modify: `DESIGN.md` §15 capability register (Badging, Haptics, Wake Lock, Web Share rows → "Shipped (B5)" with file pointers; note that pushes are payload-less by design, `lemely/web/push.py:16`, so the badge count rides the existing page handshake, not the push body)
- Test: `web/tests/unit/flashcardSession.test.ts`, `web/tests/unit/haptics.test.ts`, `web/tests/unit/share.test.ts`, `web/tests/unit/badging.test.ts` (pure, with injected `navigator`-like objects), `web/tests/unit/capabilityWiring.test.ts` (source text), `web/tests/unit/pushDecision.test.ts` (extend: `unread` passes through), `web/tests/unit/motionDefaults.test.ts` (no change expected — the landscape hint has no animation)

**Interfaces:**
- Produces: `applyGradeOutcome(session: FlashcardSessionState, outcome: { kind: "settled"; result: ReviewResultDTO } | { kind: "failed"; cardId: string; front: string; grade: ReviewGrade }): FlashcardSessionState` where `FlashcardSessionState = { results: ReviewResultDTO[]; failed: { cardId: string; front: string; grade: ReviewGrade }[]; inFlight: number }` (pure; the component advances `index` immediately in `grade()` and updates this state in `onSuccess`/`onError`; summary uses `results` only; the end screen shows "N grades still saving" while `inFlight > 0`). `haptic(kind: "tap" | "success" | "warning", nav: Pick<Navigator, "vibrate"> | undefined = typeof navigator === "undefined" ? undefined : navigator): boolean` (patterns 10 / [10, 30, 10] / [30, 20, 30]; returns false when `vibrate` is absent). `useWakeLock(active: boolean): void` (`navigator.wakeLock?.request("screen")`; re-request on `visibilitychange` → visible while `active`; release on `!active`/unmount; every failure swallowed — a wake lock is a nicety). `canWebShare(data: ShareData, nav = navigator): boolean`; `shareResult(data: { url: string; title: string; text: string }, deps?: { nav?: Navigator; toast?: (msg: string) => void }): Promise<"shared" | "copied" | "unavailable">` — `navigator.share` when `canShare`, else `clipboard.writeText(url)` + toast "Link copied" (there is no result download on `PaperResult` at `c70dd38d` to fall back to; copy-link is the honest fallback — record in the commit body). `setAppBadge(count: number, nav = navigator): Promise<boolean>` (`count > 0 ? setAppBadge(count) : clearAppBadge()`; false when unsupported). `BadgeSync()` — `useNotificationCounts()` → `setAppBadge(data?.unread ?? 0)` in an effect.
- Consumed by: Task 6 (`onShare` on `QuestionRow` via `shareResult`), Task 8 (same `CameraCapture.tsx` — sequential), Task 12 (nothing device-specific is e2e-testable here; unit + manual).

**Skills:** `error-handling-patterns`, `pwa-expert`, `technical-writer`.

- [ ] **Step 1: Re-grep** `reviewCard.mutate`, `summarizeSession`, `onConfirm`, `decidePushNotification`, `PushNotificationContent`, `useNotificationCounts` consumers, `phase === "live"`.
- [ ] **Step 2: Failing tests.** `flashcardSession.test.ts`: settled appends to `results` and decrements `inFlight`; failed appends to `failed`, not `results`; a retry of a failed card removes it from `failed` on settle. `haptics.test.ts`: no `vibrate` → false; with a spy → called with the pattern. `share.test.ts`: `share` present + `canShare` true → "shared"; absent → clipboard called, toast called, "copied"; neither → "unavailable". `badging.test.ts`: 3 → `setAppBadge(3)`; 0 → `clearAppBadge()`; unsupported → false. `capabilityWiring.test.ts` (source text): `useReviewCard` contains `retry: 3`; `FlashcardReview.tsx` imports `applyGradeOutcome` and `haptic`, and its `grade()` calls `setIndex` before `mutate` (regex `setIndex[\s\S]{0,200}reviewCard.mutate`); `confirm-modal.tsx` imports `haptic`; `CameraCapture.tsx` imports `useWakeLock` and contains `landscape:`; `PaperResult.tsx` imports `shareResult`; the three portal layouts contain `<BadgeSync`; `main.tsx` does not; `Notifications.tsx` contains `setAppBadge(0)`; `sw.ts` contains `setAppBadge`.
- [ ] **Step 3: Run** `npx vitest run tests/unit/flashcardSession.test.ts tests/unit/haptics.test.ts tests/unit/share.test.ts tests/unit/badging.test.ts tests/unit/capabilityWiring.test.ts tests/unit/pushDecision.test.ts` → FAIL.
- [ ] **Step 4: Implement; DESIGN.md capability register last.**
- [ ] **Step 5: Run** → PASS; `npm run typecheck && npm run lint && npm run build && npm run check:copy`. Manual: grade a card with the API stubbed to fail (DevTools offline) → card advances, banner names the card, Retry works; record in commit body.
- [ ] **Step 6: Ledger:** no-optimistic-flashcard-grading, zero-haptics-anywhere, wake-lock-unused-during-multi-shot-capture, web-share-unused-on-result-screen, badging-api-unused-despite-ready-data, no-orientation-policy → `done B5` (T10 is a tell with no ledger row; name it in the commit body).
- [ ] **Step 7: Commits** (three): `feat(web): optimistic flashcard grading with per-card retry banner (B5)`, `feat(pwa): haptics, wake lock during capture, web share on results (B5)`, `feat(pwa): app badge from notification counts and push handshake, landscape capture hint (B5)`.

---

### Task 8 (B5b): Scanner quality and multi-file picker

**Files:**
- Create: `web/src/lib/scanner/luma.ts` (`downscaleLuma`, `frameDelta`), `web/src/lib/scanner/edges.ts` (`sobelMagnitude`), `web/src/lib/scanner/quad.ts` (`findDocumentQuad`, `orderQuad`), `web/src/lib/scanner/homography.ts` (`homographyFromQuad`, `warpToRect`), `web/src/lib/scanner/stability.ts` (`stabilityDecision`), `web/src/lib/scanner/index.ts` (the lazy entry: `analyseFrame`, `correctPerspective`), `web/src/lib/scanner/torch.ts` (`torchSupported`, `setTorch`), `web/src/lib/pdf/assemblePages.ts` (`assemblePagesToPdf`)
- Modify: `web/src/components/CameraCapture.tsx:91-108` (delete the inline `assemblePagesToPdf`; import from `lib/pdf/assemblePages`), `:150-215` (after the stream starts: `const scanner = await import("@/lib/scanner")` once; a `requestAnimationFrame` loop sampling every 3rd frame into a 320px-wide luma buffer; `stabilityDecision` over the last 3 deltas + a found quad → auto `capturePage()` once, then re-arm after the thumbnail appears; a quad overlay `<svg>` drawn imperatively; `capturePage` runs `correctPerspective` when a quad was found, else the raw frame; torch button rendered only when `torchSupported(track)`), `web/src/components/ui/file-drop.tsx:43-60,86-145` (`multiple?: boolean`, `onFilesChange?: (files: File[]) => void`; `onFileChange` stays for single mode), `web/src/portals/student/screens/CorrectPaper.tsx` (file source uses `multiple` + `onFilesChange`; >1 image → `assemblePagesToPdf`; a PDF mixed with images → error copy "Choose either one PDF or a set of photos, not both.")
- Modify: `DESIGN.md` §15 (scanner paragraph: auto-capture, perspective correction, torch; the decision against `opencv.js`)
- Test: `web/tests/unit/scannerLuma.test.ts`, `web/tests/unit/scannerEdges.test.ts`, `web/tests/unit/scannerQuad.test.ts`, `web/tests/unit/scannerHomography.test.ts`, `web/tests/unit/scannerStability.test.ts`, `web/tests/unit/assemblePages.test.ts` (pure, synthetic `Uint8ClampedArray` frames; `assemblePages` uses `pdf-lib` in Node — it runs there), `web/tests/unit/fileDropMultiple.test.ts` (source text), `web/tests/unit/bundleBudget.test.ts` (unchanged; the build's `postbuild` proves the chunk)

**Interfaces:**
- Produces: `downscaleLuma(rgba: Uint8ClampedArray, width: number, height: number, targetWidth: number): { data: Float32Array; width: number; height: number }`; `frameDelta(a: Float32Array, b: Float32Array): number` (mean abs diff, 0..255); `sobelMagnitude(luma: Float32Array, width: number, height: number): Float32Array`; `findDocumentQuad(edges: Float32Array, width: number, height: number, options?: { threshold?: number; minAreaRatio?: number /* 0.2 */ }): Quad | null` where `Quad = [Point, Point, Point, Point]` ordered TL, TR, BR, BL by `orderQuad`; `homographyFromQuad(src: Quad, dstWidth: number, dstHeight: number): Float64Array /* 3x3 */`; `warpToRect(rgba: Uint8ClampedArray, width: number, height: number, quad: Quad, out: { width: number; height: number }): Uint8ClampedArray` (inverse-mapped bilinear sampling); `stabilityDecision(deltas: number[], quadFound: boolean, options?: { maxDelta?: number /* 2.5 */; frames?: number /* 3 */ }): "capture" | "hold"`; `analyseFrame(...)`/`correctPerspective(...)` thin wrappers over the above operating on a `CanvasRenderingContext2D`; `torchSupported(track: MediaStreamTrack): boolean` (`track.getCapabilities?.()?.torch === true`); `setTorch(track: MediaStreamTrack, on: boolean): Promise<boolean>` (`applyConstraints({ advanced: [{ torch: on }] })`). `assemblePagesToPdf(pages: readonly Blob[], options?: { filename?: string /* "scan.pdf" */ }): Promise<File>` — JPEG or PNG blobs, one page each at the image's pixel size. `FileDropProps` gains `multiple?: boolean; onFilesChange?: (files: File[]) => void`.
- **Bundle tradeoff (reviewer checks against Task 11's 150KB ceiling):** hand-rolled Sobel + largest-quad + homography is ~3–4KB gzipped inside its own lazy chunk (`scanner-*.js`); `opencv.js` is ≥1.5MB as a single wasm/JS chunk and `check-bundle-budget.mjs` gzips *every* `dist/assets/*.js`, so a lazy `opencv.js` chunk would fail the budget outright. Hand-rolled is the only option that fits; its accuracy floor (rectangular paper on a contrasting surface, ≥20% of frame) is recorded in DESIGN.md.
- Consumed by: Task 11 (`assemblePages` is the module Task 11 makes a dynamic import, moving `pdf-lib` out of the `CorrectPaper` chunk).

**Skills:** `computer-vision-pipeline`, `performance-profiling`, `error-handling-patterns`, `technical-writer`.

- [ ] **Step 1: Re-grep** `assemblePagesToPdf`, `capturePage`, `streamRef`, `onFileChange` consumers (`git grep -n 'onFileChange' web/src`).
- [ ] **Step 2: Failing tests** (synthetic frames): a flat frame → `frameDelta` 0; a frame shifted by 1px → delta > 0; a white rectangle on black → `sobelMagnitude` peaks on its border and `findDocumentQuad` returns its corners within 2px, ordered; `homographyFromQuad` of an axis-aligned rectangle maps corners exactly; `warpToRect` of a solid-colour quad yields a solid output; `stabilityDecision([1,1,1], true)` → capture, `([1,9,1], true)` → hold, `([1,1,1], false)` → hold; `assemblePagesToPdf([png, jpeg])` → a `File` with `type === "application/pdf"` and `PDFDocument.load(...).getPageCount() === 2`. `fileDropMultiple.test.ts`: `file-drop.tsx` contains `multiple` and `onFilesChange`; `CorrectPaper.tsx` contains `onFilesChange` and imports `assemblePagesToPdf` from `@/lib/pdf/assemblePages`; `CameraCapture.tsx` no longer defines `async function assemblePagesToPdf` and contains `import("@/lib/scanner")` and `torchSupported`.
- [ ] **Step 3: Run** `npx vitest run tests/unit/scannerLuma.test.ts tests/unit/scannerEdges.test.ts tests/unit/scannerQuad.test.ts tests/unit/scannerHomography.test.ts tests/unit/scannerStability.test.ts tests/unit/assemblePages.test.ts tests/unit/fileDropMultiple.test.ts` → FAIL.
- [ ] **Step 4: Implement**; profile the rAF loop on a throttled CPU (Chrome DevTools 4× slowdown): frame analysis ≤ 8ms at 320px width, else lower the sample width; record the number in the commit body.
- [ ] **Step 5: Run** → PASS; `npm run typecheck && npm run lint && npm run build`; paste `check:bundle`'s top-5 (a new `scanner-*.js` chunk must appear and be < 10KB). Manual on a phone or emulated camera: auto-capture fires on a steady sheet, the torch button appears only when supported, choosing three photos yields a 3-page PDF.
- [ ] **Step 6: Ledger:** camera-scanner-basics-only, file-picker-single-file-no-multiple → `done B5`.
- [ ] **Step 7: Commits** (two): `feat(web): scanner auto-capture, perspective correction, torch (B5)`, `feat(web): multi-file picker assembling photos into one PDF (B5)`.

---

### Task 9 (B6a): Backend — `Idempotency-Key` on `POST /student/uploads`, review-queue cursor pagination

**Files:**
- Create: `lemely/db/migrations/versions/0036_upload_idempotency_key.py` (down_revision `0035_merge_parent_invites_review`; `uploads.idempotency_key VARCHAR(128) NULL`; unique index `ux_uploads_user_idempotency` on `(user_id, idempotency_key)` `WHERE idempotency_key IS NOT NULL`)
- Modify: `lemely/db/models/attempts.py:24-50` (`Upload.idempotency_key`), `lemely/db/upload_repo.py:66-105` (`create_upload(..., idempotency_key: str | None = None)`; new `find_by_idempotency_key(*, user_id, key, since) -> OwnedUpload | None`; `IntegrityError` on the unique index → re-lookup and return the existing row), `lemely/web/routers/student.py:626-680` (`idempotency_key: Annotated[str | None, Header(alias="Idempotency-Key")] = None`; validated `^[A-Za-z0-9._-]{8,128}$` else 422; lookup within 24h before storing; on hit return the existing `paperId` with the same 200 shape)
- Modify: `lemely/db/review_repo.py:270-360` (`list_queue(..., limit: int | None = None, cursor: tuple[datetime, uuid.UUID] | None = None)` — keyset on `(created_at, id)`, existing `order_by(ReviewQueueItem.created_at)` becomes `(created_at, id)`; fetch `limit + 1` to know `has_more`), the review service (re-grep `def list_queue` outside `review_repo.py` — `lemely/web/deps.py` or a `ReviewService` module; thread `limit`/`cursor` through), `lemely/web/routers/review.py:160-184` (`limit: int = 50` (max 200), `cursor: str | None`; opaque cursor = urlsafe-base64 of `"<iso created_at>|<uuid>"`; malformed → 422), `lemely/web/schemas_review.py:68-72` (`ReviewQueueListDTO` gains `nextCursor: str | None`)
- Test: `tests/test_web_student.py` (extend: same key twice → same `paperId`, one storage upload; different keys → two rows; malformed key → 422; key older than 24h → new row), `tests/test_review_repo.py` (extend: `limit=2` returns 2 + cursor, second page continues, no overlap, no gap, stable under equal `created_at`), `tests/test_web_review.py` (extend: `?limit=1` shape has `nextCursor`; passing it pages; garbage cursor → 422; no params → identical to today's shape plus `nextCursor: null` when the queue fits)

**Interfaces:**
- Produces: header `Idempotency-Key` (optional, per-user, 24h dedupe window); `ReviewQueueListDTO { items, nextCursor }`; query params `limit` (1..200, default 50), `cursor` (opaque).
- Consumed by: Task 10 (`uploadQueue` sends the key on every attempt, page or SW), Task 11 (`useReviewQueue({ limit, cursor })`, `ReviewQueueList.nextCursor`).
- **Race documented in the commit body:** at `c70dd38d` a double-submit (double tap, or the offline queue replaying while the original request had in fact reached the server) creates two `uploads` rows and two object-storage uploads for one scan, and `POST /student/correct` then marks whichever `paperId` the client kept. The unique index closes it without a lock: two concurrent identical requests both miss the lookup, the second insert raises `IntegrityError`, the repo re-reads and returns the first row. Single-instance FastAPI, no distributed lock exists in `lemely/` and none is added.

**Skills:** `rest-api-design`, `postgresql-optimization`, `error-handling-patterns`.

- [ ] **Step 1: Re-read** `student.py:626-680`, `upload_repo.py:66-105`, `review_repo.py:270-360`, the migrations head (`ls lemely/db/migrations/versions | tail -3`), the fixture helpers at the top of `tests/test_web_review.py` and `tests/test_web_student.py:785`.
- [ ] **Step 2: Failing tests** as listed (extend the three files; follow their existing fixture pattern — they use a seeded store and the FastAPI test client).
- [ ] **Step 3: Run** `source .venv/bin/activate && pytest --no-cov tests/test_web_student.py tests/test_review_repo.py tests/test_web_review.py -q` → FAIL on the new tests.
- [ ] **Step 4: Implement**; `alembic upgrade head` against the local DB (the e2e `global-setup.ts` does the same) and `alembic downgrade -1 && alembic upgrade head` once to prove reversibility.
- [ ] **Step 5: Run** the three files → PASS; `ruff check lemely tests`; `pre-commit run --all-files`.
- [ ] **Step 6: Ledger:** review-queue-unbounded-unvirtualized stays `pending` until Task 11 ships the load-more; no row closes here (offline-1/offline-3 close in Task 10). Add evidence text to `offline-1` ("backend half: idempotency key, Task 9, `pytest --no-cov tests/test_web_student.py`") without changing its status.
- [ ] **Step 7: Commits** (two): `feat(api): Idempotency-Key on student uploads, deduped per user for 24h (B6)`, `feat(api): cursor pagination on the teacher review queue (B6)`.

---

### Task 10 (B6b): Offline upload queue, Background Sync, persisted query cache

**Files:**
- Create: `web/src/lib/offline/uploadQueue.ts` (IDB store, DOM-free so `sw.ts` can import it), `web/src/lib/offline/queueDecision.ts` (pure `shouldQueueUpload`, `nextQueueAction`), `web/src/lib/offline/useUploadQueue.ts` (page hook: drain on mount, on `online`, and when the SW posts `{ type: "UPLOAD_QUEUE_DRAINED" }`), `web/src/lib/offline/persistAllowlist.ts` (pure `isPersistableQueryKey`), `web/src/lib/offline/queryPersister.ts` (`idb-keyval` + `createAsyncStoragePersister`), `web/src/components/ui/queued-banner.tsx` (`QueuedBanner`)
- Modify: `web/src/sw.ts:121-175` (inside `if (precacheEnabled)`: `new Queue("lemely-uploads", { onSync: drainUploadQueueFromWorker })` from `workbox-background-sync` — `Queue` with a custom `onSync`, not `BackgroundSyncPlugin`: the plugin replays raw requests and discards their responses, but this flow needs the returned `paperId` to then call `/student/correct`, so the SW drains the shared IDB queue itself and posts the result to open clients), `web/src/main.tsx:89-118` (`QueryClientProvider` → `PersistQueryClientProvider` with `persistOptions={{ persister, maxAge: 24 * 60 * 60 * 1000, buster: currentBuildId(), dehydrateOptions: { shouldDehydrateQuery: (q) => isPersistableQueryKey(q.queryKey) } }}`), `web/src/portals/student/screens/CorrectPaper.tsx` (on an offline-classified upload failure — `isOfflineFailure` from `lib/offlineFailure.ts` — enqueue `{ id, idempotencyKey, scan, markScheme, createdAt }` and render `<QueuedBanner>`; the existing `OfflineState` path stays for non-upload failures), the upload hook (re-grep `"/student/uploads"` in `web/src/lib/hooks/useStudentApi.ts`; it gains an `idempotencyKey` argument sent as the `Idempotency-Key` header — confirm `request()` in `lib/api.ts` forwards `init.headers`), `web/package.json` (add `workbox-background-sync@^7.4.1`, `@tanstack/react-query-persist-client`, `@tanstack/query-async-storage-persister` matching the installed `@tanstack/react-query` major, `idb-keyval`; `npm install` to relock), `web/vite.config.ts` (nothing unless `globIgnores` needs it — it does not)
- Modify: `DESIGN.md` §15 "Offline behaviour" (queue semantics, what is and is not persisted, and the `emp-offline-staging-by-design` note: staging never precaches by `PRECACHE_HOSTS` design, so offline on staging is expected to fail — documented, not fixed)
- Test: `web/tests/unit/queueDecision.test.ts`, `web/tests/unit/persistAllowlist.test.ts` (pure), `web/tests/unit/uploadQueue.test.ts` (the store against an injected in-memory `idb-keyval`-shaped adapter — `uploadQueue.ts` takes `{ get, set, del, keys }` with the real `idb-keyval` as default), `web/tests/unit/offlineWiring.test.ts` (source text), `web/tests/unit/swSource.test.ts` (extend: `Queue` construction sits inside the `precacheEnabled` block; no `BackgroundSyncPlugin`)

**Interfaces:**
- Produces: `type QueuedUpload = { id: string; idempotencyKey: string; scan: Blob; scanName: string; markScheme: Blob | null; createdAt: number; attempts: number; status: "queued" | "uploaded"; paperId?: string }`; `enqueueUpload(entry: Omit<QueuedUpload, "attempts" | "status">, store?): Promise<void>`; `listQueuedUploads(store?): Promise<QueuedUpload[]>`; `markUploaded(id, paperId, store?)`; `removeQueued(id, store?)`; `drainUploadQueue(deps: { upload: (entry: QueuedUpload) => Promise<{ paperId: string }>; correct: (paperId: string) => Promise<void>; store? }): Promise<{ drained: string[]; failed: string[] }>` — used by both the page hook and the SW `onSync`, with the fetch implementations injected. `shouldQueueUpload(error: unknown): boolean` (offline classification only — a 4xx/5xx is never queued). `nextQueueAction(entry: QueuedUpload, now: number): "upload" | "correct" | "drop"` (`drop` after 24h or 5 attempts). `isPersistableQueryKey(key: readonly unknown[]): boolean` — allowlist by first segments: `["reference"]`, `["student","classes"]`, `["announcements"]`, `["notifications"]`, `["flashcards","decks"]`, `["teacher","classes"]`, `["me","profile"]`; **explicitly false** for `["student","overview"]`, `["student","subject", …]`, `["student","result", …]`, `["teacher","review", …]` and any key not on the allowlist (re-grep the real key literals in `useStudentApi.ts`, `useReferenceApi.ts`, `useAnnouncementApi.ts`, `useNotificationApi.ts`, `useFlashcardApi.ts`, `useTeacherApi.ts`, `useMeApi.ts` and use those; the test asserts against a source-text extraction of each hook's `queryKey` so the allowlist and the hooks cannot drift). `QueuedBanner({ count, onRetry }: { count: number; onRetry: () => void })` — `role="status"`, copy "1 scan is waiting for a connection. It will upload on its own." / "N scans are waiting…".
- Consumed by: Task 11 (same `CorrectPaper.tsx`), Task 12 (nothing — offline e2e is out of Playwright scope here; unit + manual).

**Skills:** `pwa-expert`, `caching-strategies`, `error-handling-patterns`, `technical-writer`. Reviewer additionally `security-auditor` (IDB holds scan blobs: cleared on sign-out — hook `clearUploadQueue()` into the existing logout path in `AuthContext.tsx`).

- [ ] **Step 1: Re-read** `sw.ts:100-180`, `main.tsx:89-118`, `lib/api.ts` `request()`, `lib/offlineFailure.ts`, `AuthContext.tsx` logout, `CorrectPaper.tsx` upload path; run `npm ls @tanstack/react-query` to pin the persist-client version.
- [ ] **Step 2: Failing tests** as listed; `offlineWiring.test.ts`: `main.tsx` contains `PersistQueryClientProvider` and `isPersistableQueryKey`; `sw.ts` imports `Queue` from `workbox-background-sync` and `drainUploadQueue`; `CorrectPaper.tsx` imports `enqueueUpload`, `QueuedBanner`, `shouldQueueUpload`; `AuthContext.tsx` calls `clearUploadQueue`.
- [ ] **Step 3: Run** `npx vitest run tests/unit/queueDecision.test.ts tests/unit/persistAllowlist.test.ts tests/unit/uploadQueue.test.ts tests/unit/offlineWiring.test.ts tests/unit/swSource.test.ts` → FAIL.
- [ ] **Step 4: Implement; DESIGN.md last.**
- [ ] **Step 5: Run** → PASS; `npm run typecheck && npm run lint && npm run build`; `grep -c 'lemely-uploads' dist/sw.js` ≥ 1. Manual: DevTools offline, submit a scan → banner; go online → uploads, marks, banner clears; reload while offline → Overview shows `OfflineState` (not stale grades), Classes shows cached list. Record in the commit body.
- [ ] **Step 6: Ledger:** offline-1, offline-3, sw-background-sync-upload-candidate, sw-offline-production-trace, emp-offline-staging-by-design (documented) → `done B6` (T12 is a tell with no ledger row; name it in the commit body).
- [ ] **Step 7: Commits** (two): `feat(pwa): offline upload queue with Background Sync drain and queued banner (B6)`, `feat(web): persisted query cache allowlisted by key, grades never persisted (B6)`.

---

### Task 11 (B6c): Prefetch-on-intent, `pdf-lib` split, 150KB budget, review load-more

**Files:**
- Modify: `web/src/portals/student/index.tsx` (the `lazy(() => import("./screens/CorrectPaper"))` becomes `const loadCorrectPaper = () => import("./screens/CorrectPaper")`, `lazy(loadCorrectPaper)`; `Header` CTA, `BottomActionBar` and the BottomNav "Correct" item get `onPointerEnter`/`onTouchStart`/`onFocus` → `loadCorrectPaper()` via a shared `prefetchOnIntent(load)` helper in `web/src/lib/prefetchOnIntent.ts` that fires once), `web/src/components/ui/nav-shells.tsx` (`NavShellItem.prefetch?: () => void` spread onto the three intent events), `web/src/components/ui/bottom-action-bar.tsx` (`prefetch` prop)
- Modify: `web/src/components/CameraCapture.tsx` (from Task 8: `import { assemblePagesToPdf } from "@/lib/pdf/assemblePages"` → `const { assemblePagesToPdf } = await import("@/lib/pdf/assemblePages")` at the call site), `web/src/portals/student/screens/CorrectPaper.tsx` (same for its multi-file path)
- Modify: `web/scripts/check-bundle-budget.mjs:13-27` (`DEFAULT_BUDGET_KB = 150`; header comment updated: "Phase B lowered this from 200 after splitting `pdf-lib` and the scanner out of `CorrectPaper`"), `DESIGN.md` §15 "Mechanical enforcement" (150KB)
- Modify: `web/src/lib/hooks/useTeacherApi.ts:336-353` (`useReviewQueue(params?: { classId?; reason?; minAgeHours?; limit?: number; cursor?: string })`; query key gains `limit`/`cursor`), `web/src/lib/teacherTypes.ts:684-686` (`ReviewQueueList.nextCursor: string | null`), `web/src/portals/teacher/screens/Review.tsx:202-215,495-510` (pages accumulate in local state keyed by the filter tuple — reset with the existing `useEffect` at :210; "Load more" `Button` below the table when `nextCursor`; **no virtualization** — deferred by design, say so in a comment on the button)
- Test: `web/tests/unit/prefetchOnIntent.test.ts` (pure: fires `load` once across repeated calls), `web/tests/unit/prefetchWiring.test.ts` (source text: `student/index.tsx` contains `loadCorrectPaper` used by both `lazy(` and a `prefetch`; `CameraCapture.tsx` and `CorrectPaper.tsx` contain `import("@/lib/pdf/assemblePages")` and no static `from "pdf-lib"`; `check-bundle-budget.mjs` has `DEFAULT_BUDGET_KB = 150`), `web/tests/unit/bundleBudget.test.ts` (extend: `overBudget` at 150), `web/tests/unit/reviewPaging.test.ts` (pure `mergeReviewPages(prev, next, resetKey)` extracted from `Review.tsx`)

**Interfaces:**
- Produces: `prefetchOnIntent(load: () => Promise<unknown>): { onPointerEnter: () => void; onTouchStart: () => void; onFocus: () => void }`; `mergeReviewPages(prev: { key: string; items: ReviewQueueItem[] }, page: { key: string; items: ReviewQueueItem[] }): { key: string; items: ReviewQueueItem[] }` (replaces when `key` differs, appends deduped by `itemId` otherwise).
- Consumes: Task 4 `BottomActionBar`/`NavShellItem`, Task 8 `assemblePages`, Task 9 `nextCursor`/`limit`/`cursor`.

**Skills:** `performance-profiling`, `react-performance-optimizer`, `caching-strategies`.

- [ ] **Step 1: Re-read** the current `check:bundle` top-5 (`npm run build` on the branch as it stands — Tasks 8 and 10 changed it) and paste it in the commit body as the "before".
- [ ] **Step 2: Failing tests** as listed.
- [ ] **Step 3: Run** `npx vitest run tests/unit/prefetchOnIntent.test.ts tests/unit/prefetchWiring.test.ts tests/unit/bundleBudget.test.ts tests/unit/reviewPaging.test.ts` → FAIL.
- [ ] **Step 4: Implement.** If after the `pdf-lib` split `CorrectPaper-*.js` is still > 150KB gzipped, read `npx vite build --mode production` with `build.rollupOptions.output.manualChunks` unset and inspect `CorrectPaper.tsx`'s import graph (`npx vite-bundle-visualizer` is not a dependency — use `rollup-plugin-visualizer` via `npx` once, do not add it) to find the next heaviest import and lazy it the same way; the plan's expectation is `CorrectPaper` ≈ 40–60KB after `pdf-lib` (~120KB gzipped) leaves it.
- [ ] **Step 5: Run** → PASS; `npm run typecheck && npm run lint && npm run build` (the `postbuild` budget at 150 must pass — paste the "after" top-5); `npx vitest run tests/unit/checkNativeInvariants.test.ts` still green. Manual: hover the CTA on desktop → Network shows `CorrectPaper-*.js` fetched before click; Review with `limit=1` in a query override shows "Load more".
- [ ] **Step 6: Ledger:** correct-paper-chunk-no-prefetch, review-queue-unbounded-unvirtualized → `done B6`.
- [ ] **Step 7: Commits** (two): `perf(web): prefetch CorrectPaper on intent, lazy pdf-lib, bundle budget 150KB (B6)`, `feat(web): review queue load-more on cursor pagination (B6)`.

---

### Task 12 (B7): Native-feel e2e spec and installability check

**Files:**
- Modify: `web/playwright.config.ts:129-134` (projects: keep `chromium` with `testIgnore: /native-feel\.spec\.ts/`; add `{ name: "iphone-15", use: { ...devices["iPhone 15"] }, testMatch: /native-feel\.spec\.ts/ }` and `{ name: "pixel-7", use: { ...devices["Pixel 7"] }, testMatch: /native-feel\.spec\.ts/ }`; Playwright 1.62's `iPhone 15` profile is WebKit-based — if the sandbox has no WebKit build (`npx playwright install webkit` fails), use `{ ...devices["iPhone 15"], defaultBrowserType: "chromium" }` and record it)
- Create: `web/e2e/native-feel.spec.ts`, `web/e2e/native-feel.helpers.ts` (`signInAs(page, "student" | "teacher")` reusing `readSeed()` and the `/login` form pattern from `reduced-motion.spec.ts:55-66`; `interactiveTargets(page)`; `failedRequests(page)` built on `watchConsole` from `e2e/console-errors.ts`)
- Create: `web/scripts/check_installability.mjs` (Chromium launched with `--host-resolver-rules=MAP lemelyig.com 127.0.0.1` and `--unsafely-treat-insecure-origin-as-secure=http://lemelyig.com:4173`, `vite preview --host 127.0.0.1 --port 4173` from `serve_guard.mjs`'s `startPreview`, CDP `Page.getInstallabilityErrors` after the SW registers; wrapped in `withRetry(fn, { attempts: 3 })` from `scripts/nav_retry.mjs`; exits 1 on any installability error. Lighthouse itself cannot do this: `audit.mjs:210-214` records that Google removed the PWA category and its `installable-manifest` audit, so "Lighthouse installability" is met by the CDP call the audit used to wrap), `web/package.json` (`"check:installable": "node scripts/check_installability.mjs"`)
- Modify: `web/e2e/reduced-motion.spec.ts` (unchanged assertions; add a one-line comment pointing at the broadened coverage in `native-feel.spec.ts`)
- Modify: `DESIGN.md` §15 "Mechanical enforcement" (e2e spec + installability script named)
- Test: the spec is the test. Unit: `web/tests/unit/nativeFeelSpec.test.ts` (source text: the spec names all five routes, both device projects exist in `playwright.config.ts`, the spec asserts `requestfailed`)

**Assertions in `native-feel.spec.ts` (each its own `test`, both device projects):**
1. `/login`, `/signup`, `/reset`, `/join`, `/settings/notifications` (the last signed in as student): every `input, textarea, select` has computed `font-size` ≥ 16px.
2. Same five routes + `/student` (signed in): every visible `button, [role="button"], nav a, label` and every `a` not inside a `p` has a bounding box height ≥ 44 and width ≥ 44 (icon-only) or height ≥ 44 (text); failures list the element's accessible name.
3. Computed styles prove the chrome suppression: `getComputedStyle(document.body).webkitTapHighlightColor` is `rgba(0, 0, 0, 0)`; `getComputedStyle(firstButton).touchAction === "manipulation"` (device profiles set `hasTouch`, so `(pointer: coarse)` matches); `getComputedStyle(document.documentElement).overscrollBehaviorY === "contain"`.
4. Count of CSS rules whose `cssText` includes `safe-area-inset` across same-origin stylesheets > 0; `meta[name=viewport]` content equals `width=device-width, initial-scale=1.0, viewport-fit=cover, interactive-widget=resizes-content` exactly.
5. Signed in as student on `/student`: open the drawer ("Open student navigation"), `page.goBack()` → `[role=dialog]` gone, `page.url()` unchanged. On `/student/flashcards/<seed subject>`: open "Delete deck" (a `ConfirmModal`), `page.goBack()` → dialog still visible, URL unchanged, and the deck is not deleted (row still present after `page.reload()`). For a *dismissible* `Modal`: re-grep `<Modal` in `web/src/portals/student` for one reachable from seed data (e.g. the card editor in `FlashcardDecks.tsx:217`); open it, back → closed, URL unchanged; if none is reachable from seed data, assert the drawer case only and say so in the spec.
6. On `/student` scroll to `y = 600`, tap the Classes tab, tap back (browser) → `window.scrollY` within 50px of 600 (POP restores); tapping Overview → Classes fresh scrolls to 0.
7. Signed out, `page.goto("/student/notifications")` → redirected to `/login…`; sign in → lands on `/student/notifications`.
8. `page.emulateMedia({ reducedMotion: "reduce" })`: on `/student` every element with a non-`none` `animation-name` has `animation-duration` ≤ 10ms and every `transition-duration` ≤ 10ms; open and close the drawer → `[role=dialog]` count is 0 after one `requestAnimationFrame` (Task 3's synchronous unmount).
9. Cold load of `/login` and, signed in, of `/student`: zero `requestfailed` events and zero responses with `status >= 400` (the `pushConfigColdLoad` regression from A7 stays covered here too).
10. (Optional, not gating) with `route.continue` delayed by 800ms on the `CorrectPaper` chunk, `/student/correct` shows `[data-tier="skeleton"]` (Task 1 Behaviour 1) before the heading.

**Skills:** `playwright-e2e-tester`, `webapp-testing`, `technical-writer`.

- [ ] **Step 1: Re-read** `playwright.config.ts` whole, `e2e/seed.ts` (`readSeed`, `SeedContract` fields: which subject the seeded student owns decks in), `e2e/console-errors.ts`, `scripts/nav_retry.mjs`, `scripts/serve_guard.mjs:71-110`; check `npx playwright install --dry-run webkit`.
- [ ] **Step 2: Write the spec and `nativeFeelSpec.test.ts`;** run `npx vitest run tests/unit/nativeFeelSpec.test.ts` → FAIL until the config and spec exist.
- [ ] **Step 3: Run** `npx playwright test e2e/native-feel.spec.ts --project=pixel-7` with the backend up → expect real failures on any assertion the implementation does not yet satisfy; fix the implementation (in the owning packet's files, small commits scoped `fix(web): … (B7 review fix)`), never weaken the assertion.
- [ ] **Step 4: Run** `npx playwright test e2e/native-feel.spec.ts` (both projects) → PASS; `node scripts/check_installability.mjs` → `installable: no errors`; `npx vitest run tests/unit/nativeFeelSpec.test.ts` → PASS; `npm run typecheck && npm run lint && npm run build`. If the backend cannot be started locally, run at least the signed-out tests (1–4 on `/login`, 9) and record "signed-in cases deferred to CI" in the commit body.
- [ ] **Step 5: CI:** re-grep `.github/workflows/ci.yml` for the Playwright job; if the e2e job exists, ensure it installs the device browsers (`npx playwright install --with-deps chromium webkit`) and runs `check:installable`; if no e2e job exists, add `npm run check:installable` to the web job after `npm run build` and leave e2e to the existing arrangement (record which).
- [ ] **Step 6: Ledger:** x-motion-reduced-motion-e2e-narrow-scope → `done B7` (evidence: "e2e half; unit half stays with C4"); the dossier's "How to measure it" native-feel section has no ledger row — name it in the commit body.
- [ ] **Step 7: Commits** (two): `test(e2e): native-feel spec on iPhone 15 and Pixel 7 emulation (B7)`, `ci(web): installability check against a production-hostname preview (B7)`.

---

### Task 13: Phase close — review, verification, PR

- [ ] **Step 1: Opus review of the whole branch diff** (`code-reviewer`, skills `code-review-checklist`, `security-auditor` for Tasks 9–10): `git diff develop...HEAD`. Fix critical/high in follow-up commits per packet scope.
- [ ] **Step 2: `ai-slop-cleaner` pass** on the diff (comments that restate code, dead flags, placeholder copy, leftover `min-[820px]`).
- [ ] **Step 3: Verifier** (`verifier`, opus): re-run every Step 5 command above, `node scripts/check-native-invariants.mjs` (20 checks), `npm run build` (150KB budget), `npm run check:copy`, `node scripts/check_installability.mjs`; confirm the ledger has no `pending` row with `phase = B`; replace every `done B<n>` with `done <short sha>`; recount the ledger's summary tables at the bottom of the file.
- [ ] **Step 4: `merge-readiness` skill report**; `superpowers:finishing-a-development-branch`.
- [ ] **Step 5: Push and open PR** into `develop`:
```bash
git push -u origin feat/native-structural-capability
gh pr create --base develop --title "feat: navigation model, gestures, capability APIs, offline queue (audit remediation phase B)" --body-file /tmp/claude-1000/-home-sico-Code-Lemely/459a483f-eff5-4286-a616-5593319861ac/scratchpad/pr-b.md
```
PR body: packet list B1–B7 with ledger row counts, the two `already-fixed` rows from Task 0, the bundle before/after, the manual device checklist (installed splash on iOS, standalone edge-swipe, back dismisses drawer, ConfirmModal survives back, offline queue replays, torch button on a supported phone, app badge), and the session URL as the last line.
- [ ] **Step 6: Hand off** — user merges; Phase C plan is written against merged `develop`.

---

## Self-review (done while writing)

- **Spec coverage:** every Phase B packet in the design spec (B1–B7) maps to Tasks 1–12; all 42 `phase = B` ledger ids appear in exactly one task's Step 6 (B1: 6 incl. the two `already-fixed` in Task 0; B2: 9 across Tasks 2–4; B3: 6 in Task 4; B4: 5 in Task 6; B5: 8 across Tasks 7–8; B6: 7 across Tasks 10–11 (Task 9 adds evidence only); B7: 1 in Task 12). Tells T8/T10/T12 have no ledger rows; named in commit bodies.
- **Deviations from the brief, each with the reason in the task:** pre-mount shell already landed (Task 0); `ScrollRestoration` lives in a root layout route, not `main.tsx` (Task 2); push is payload-less by design so the badge count rides the page handshake, backend untouched (Task 7); no deck-rename API so "Rename" is omitted (Task 6); no result download exists so Web Share falls back to copy-link (Task 7); `workbox-background-sync` `Queue` with custom `onSync` instead of `BackgroundSyncPlugin` (Task 10); Lighthouse no longer audits installability, replaced by a CDP `Page.getInstallabilityErrors` script with the same retry wrapper (Task 12); `useDragGesture` is created in Task 4 (B3) because edge-swipe is its first consumer, so B4 runs after B3 rather than in parallel with B1/B5; the student sidebar's `min-[820px]` arbitrary value is replaced by a `sidebar` breakpoint token (Task 4) since §14 rule 3 forbids the literal.
- **Placeholders:** none — each step names files, values, tests and commands. Code bodies intentionally absent (memory `delegate-by-model`): implementers author code, opus reviews.
- **Type consistency:** `useDragGesture(ref, options)` (Task 4) is what Tasks 5 and 6 call; `DragGestureOptions.axis`/`onCommit(dx, dy)`/`startFilter`/`enabled` are the fields Tasks 5–6 use; `shouldCommitDrag(dx, dy, axis, threshold)` is used by `edgeSwipeDecision` and `usePullToRefresh`; `NavShellItem` final shape (Task 4) includes `prefetch` added by Task 11; `RouteSkeleton` (Task 1) is what `RootOutlet`/`ScreenOutlet` (Tasks 2–3) leave in place; `onClose` contract (Task 2) is what Task 3's `overlayPhase` and Task 6's drag-dismiss end in; `assemblePagesToPdf(pages: readonly Blob[])` (Task 8) is what Task 11 lazy-imports; `ReviewQueueList.nextCursor` (Task 9 DTO, Task 11 type) match; `isPersistableQueryKey` excludes exactly the keys the brief names plus `result`/`review` (grade-bearing).
- **Deferred by design:** virtualization of the review queue; C4's unit half of the reduced-motion row; opencv.js.
